# claude-loom local state (Phase 1 以降で daemon が作る)
.claude-loom/
.superpowers/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Node (M1 以降)
node_modules/
*.log
npm-debug.log*

# Test artifacts
.coverage
coverage/

# Playwright (M3.1 から、ui/e2e baseline は track するが run output は除外)
test-results/
playwright-report/
.last-run.json

# Playwright MCP (browser_* tool 経由の ad-hoc smoke test 残骸、git track しない)
# M0.11.3 の loom-ui-smoke skill が docs/smoke-tests/<date>-<scope>/ に
# 構造化保存する SSoT を確立予定 (未実装)
.playwright-mcp/
/smoke-*.png

# Build output
dist/
build/

# Env
.env
.env.local

# SQLite (daemon, M1 以降)
*.db
*.db-shm
*.db-wal

# M1 daemon
*/node_modules/
*/dist/
*.tsbuildinfo
