/**
 * App — top-level component placeholder.
 * Tasks 4-9 will replace this with AppShell + routing.
 */
export function App(): JSX.Element {
  return (
    <div>
      <h1>claude-loom UI</h1>
    </div>
  );
}
