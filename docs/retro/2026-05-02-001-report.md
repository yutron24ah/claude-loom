# Retro Report — 2026-05-02-001

**retro_id**: `2026-05-02-001`
**project_id**: `claude-loom`
**mode**: conversation
**scope**: M0.14 (partial) / M1 / M1.5 / M2.0 / M2 — 直近 5 milestone (前回 retro 2026-04-29-001 = M0.13 完了時 以降の全期間)
**HEAD at retro start**: `5c5c2d4 docs(plan): mark m2-t9 done (M2 acceptance test)` (branch `feat/m2-ui-shell`、main から 26 commit ahead)
**generated_at**: 2026-05-02
**executor note**: **`[degraded-protocol]` Task tool 不在のため retro-pm 単体で 4-lens sequential 評価モードで実行**。前回 retro 2026-04-29-001 の proc-002 (Task tool fallback) で codified された discipline に従い、(a) ユーザーへ即時通知済 (b) sequential degraded mode 明示的 fallback 宣言済 (c) 本 report に degraded-protocol タグ付与。並列 dispatch / counter-argument 独立性は失われとる、findings の rigor は通常 retro より低い可能性を留意。

---

## Summary

| stage | 件数 | 備考 |
|---|---|---|
| Stage 1 raw findings | 14 | 4 lens 横断（pj=4 / process=6 / researcher=2 / meta=3）+ user-axis 6 ほぼ process-axis に吸収 |
| Stage 2 input | 14 | 全件 counter-argument 対象（degraded mode = self counter-argument） |
| Stage 2 confirm | 6 | pj-2 / proc-1 / proc-3 / proc-6 / meta-1 / meta-3 |
| Stage 2 for_downgrade | 5 | pj-1 / pj-3 / proc-2 / proc-4 / proc-5 / meta-2 / res-1（severity 軽減） |
| Stage 2 for_drop | 2 | pj-4（M3 deferral 既知）/ res-2（外部確証不足） |
| Stage 3 final（archive） | 11 | severity 順、user 提示対象 |

---

## Findings by lens

### pj-axis (3 件、1 件 drop)

#### finding-pj-001 [for_downgrade / risk: low (originally low) / category: spec-drift-doc-update]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: PLAN.md frontmatter `last_synced_at: 1777194000000` (2026-04-25 想定) が **prior retro 2026-04-29-001 の pj-001 で同 finding 提出済、本 retro までに更新されてへん**。M0.14 partial / M1 / M1.5 / M2.0 / M2 を経過後も未更新。
- **counter-argument verdict**: for_downgrade (M1 daemon の plan_items table 同期で auto-update する設計、M3 で frontmatter polling 機構実装まで manual update 不可避、severity 維持で auto-apply 推奨)
- **applicable file**: `PLAN.md` line 4
- **proposal**: `last_synced_at: 1778025600000` (2026-05-02 想定 epoch ms) に更新、本 retro と同時 commit
- **guidance_proposal**: null

#### finding-pj-002 [confirm / risk: medium / category: spec-drift-architectural]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: `README.md` line 197-209 の M2 frontend 接続例が **実装と乖離**。README は `createTRPCClient` from `@trpc/client` + `httpBatchLink` + `wsLink` + `x-loom-token` headers の構成を documented しとるが、実装 (`ui/src/trpc/client.ts:12-13`) は `createTRPCReact` from `@trpc/react-query` + `wsLink` のみ、`httpBatchLink` と auth token は **意図的に M5 polish へ defer** (code comment 内に明記)。reader が README に従うと動かんコードを書く。
- **counter-argument verdict**: confirm (新規 contributor が README 通り実装すると tRPC v11 + React Query 統合パターンに合わず壊れる、reader-facing 重要度 medium)
- **applicable file**: `README.md` lines 191-213
- **proposal**: README §M2 を実 M2 実装に合わせて書き直し OR "本サンプルは Phase 2 production 接続例 (M5 polish 後)、M2 dev mode は createTRPCReact 使用、`ui/src/trpc/client.ts` 参照" と明示的 caveat 追加
- **guidance_proposal**: null

#### finding-pj-003 [for_downgrade / risk: low (originally medium) / category: feature-gap]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: M0.14 が m0.14-t6 (DOC_CONSISTENCY_CHECKLIST 更新) / t7 (process-judge 3 新 category test 拡張) / t8 (tag + merge) を `[ ] todo` のまま残し、M1 / M1.5 / M2.0 / M2 が直線的に追い越し進行。`m0.14-complete` タグ無し、PLAN.md `m0`〜`m0.13-complete` 全保持と書いとるが M0.14 完成基準は満たしとらん。M0.14 自体は **doc-only milestone** で内容 (CLAUDE.md skill mandate/suggest 再構成、SPEC §3.10.1 SSoT 化) は実質完了済、残ってるんは meta clean-up のみ。
- **counter-argument verdict**: for_downgrade (M0.14 essence は SPEC §3.10.1 / CLAUDE.md に反映済、t6/t7/t8 は cleanup task、severity を medium → low に下げ)
- **applicable file**: 残 3 task の処理 — `docs/DOC_CONSISTENCY_CHECKLIST.md`、`tests/agents_test.sh` (or skip)、git tag `m0.14-complete`
- **proposal**: M2 完了 tag 設置と同タイミングで M0.14 残 task を closing。または PLAN.md M0.14 セクションに「t6/t7/t8 は cleanup deferred、M0.14 essence 反映済」と明記して PLAN を SSoT に整合させる
- **guidance_proposal**: "milestone 進行 (Mn → Mn+1) 前に **直前 milestone の完成基準と PLAN.md status を整合確認**。tag 設置 / 残 todo / 完成基準 8 条件、3 段全部 cross-check してから次 milestone に進む。drift を検出したら user に共有し fast-track close する。"

#### finding-pj-004 [for_drop] (originally pj-axis)

- **description**: SPEC §3.6 WebSocket event 一覧に `subagent_started/finished/session_ended` 等あるが M2 toast UI は 5 event MVP (`daemon_disconnected/reconnected/consistency_finding_new/subagent_failed/project_added`) のみ実装、`subagent_started/finished` toast 化されてへん。
- **counter-argument verdict**: for_drop (M2 plan Task 8 で「subagent_failed は M3 で WS event subscribe 後に実接続、M2 では toast UI のみ確認」と既知の M3 deferral として明記、SPEC §3.6 は 完全表 で MVP scope ではない)

### process-axis (6 件、0 件 drop)

#### finding-proc-001 [confirm / risk: high / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-developer"]`
- **description**: M2 Task 5 / 6 / 7 / 8 で **GREEN commit handoff anomaly 4 連発** (user context 明示)。loom-developer subagent が reviewer verdict pass 後に working tree に impl ファイル放置、`git commit` 実行せず final report (reviewer JSON) を返してセッション終了。PM が補完 commit を実施。Task 7 では明示的「両 commit を dev 自身が完遂、self-verify 必須」prompt しても効果なし → **loom-developer agent definition の終了 flow に構造的 bug**。
- **counter-argument verdict**: confirm (再現性 4 task 連続、user 重要度 high 明示、Task 9/10/11 は Strategy b で 0 anomaly = pattern 確実)
- **applicable file**: `agents/loom-developer.md` Step 8 (commit step) + 終了 condition の明確化
- **proposal**:
  1. `agents/loom-developer.md` Step 8 (or 該当 step) に **「reviewer verdict pass 後、必ず以下を順序実行: (1) `git status` で staged/unstaged 確認 (2) `git add <files>` (3) `git commit -m "<conventional prefix>: <subject>"` (4) `git log -1 --oneline` で commit hash と subject 確認 (5) final report を返す」** と明記。
  2. final report template に `committed_sha: <40-char-sha>` field 必須化、SHA 不在 → invalid response として PM が refuse + retry。
- **guidance_proposal**: "loom-developer 終了時、reviewer verdict pass 直後に必ず git commit を実行し、final report に committed_sha を含めよ。SHA 不在の終了は protocol violation として再 dispatch される。「commit せずに reviewer pass を返す」は完了状態じゃない。"

#### finding-proc-002 [for_downgrade / risk: medium (originally medium) / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm", "loom-developer"]`
- **description**: M2 Task 9/10/11 で採用された **Strategy b**（`## CRITICAL: NO GIT COMMIT` 明示で dev は code + reviewer dispatch + final report のみ、PM が RED+GREEN 統合 commit）が proc-001 を mitigate した実証あり (Task 9 = 3 subagent 並列、最大 workload で 0 anomaly)。一方で SPEC §3.8.1 「atomic commits = 1 commit = 1 論理変更 / dev が test 書いて commit、その時点で red の理念」から逸脱する normalization。「dev が commit せん」を default にすると TDD red commit 規律 (M0.13 codified) と緊張関係。
- **counter-argument verdict**: for_downgrade (Strategy b は proc-001 mitigation の pragmatic 解、severity medium → 「策定された discipline として明文化、condition 明示」に転換)
- **applicable file**: `agents/loom-pm.md` workflow + `agents/loom-developer.md` 双方 + `docs/COMMIT_GUIDE.md` 補足
- **proposal**: Strategy b を「**parallel batch / heavy workload (≥3 subagent / 9+ files) 時の代替 commit pattern**」として正式化。Single dev / 単純 task では従来 Strategy a (dev 自身 commit) を keep、選択基準を agent prompt に明記。dev 終了時 `committed_sha: null + commit_handoff: pm` を report に含めて PM に明示移譲。
- **guidance_proposal**: "Task dispatch 前に commit pattern を選択せよ — single subagent / 単純 task は Strategy a (dev 自身 commit、proc-001 fix 込)、parallel batch (≥3 subagent) や heavy workload は Strategy b (PM 統合 commit)。final report に commit_handoff: dev | pm を必ず明記。"

#### finding-proc-003 [confirm / risk: medium / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-retro-pm", "loom-pm"]`
- **description**: M0.13 で codify された **「milestone tag 設置直後の retro session で、直近 reviewer dispatch の verdict (commit / output ref) を `pending.json` の `verdict_evidence` field に保存」** hook が、本 retro session で **未実施**。M2 Task 5-11 で reviewer pass を確認 (user context 明示) したが、verdict JSON / commit ref が `<project>/.claude-loom/retro/2026-05-02-001/` に保存されてへん。「review skip」と「指摘ゼロ」の判別不能 — 前回 retro で proc-004 として上げた懸念と同 pattern。
- **counter-argument verdict**: confirm (codified discipline の不実行、本 session が degraded mode だったとはいえ verdict 保存は機械的に実行可能、user explicit 指摘あり)
- **applicable file**: `agents/loom-retro-pm.md` Step 1 直後 + `agents/loom-pm.md` milestone tag 設置 hook
- **proposal**:
  1. `agents/loom-retro-pm.md` の Stage 0 (retro_id 採番直後) に **「直前 implementation phase の reviewer dispatch ログを `<project>/.claude-loom/retro/<retro_id>/verdict_evidence/` に保存 (commit_sha / verdict / aspect-level findings)」** step 追加。
  2. PM agent の milestone tag 設置 hook に「retro 起動時に reviewer JSON を tag-time snapshot として渡せ」と明記。
- **guidance_proposal**: "retro session 開始直後 (retro_id 採番後 / Stage 1 dispatch 前) に、直前 milestone の reviewer dispatch evidence (commit SHA / reviewer JSON output / aspect verdict) を `<project>/.claude-loom/retro/<retro_id>/verdict_evidence.json` に保存せよ。これで「review skip」と「指摘ゼロ pass」を retro 中 / 後の audit で区別できる。"

#### finding-proc-004 [for_downgrade / risk: low (originally medium) / category: process-commit-granularity]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm"]`
- **description**: M0.14 milestone が m0.14-t6/t7/t8 を todo 残しのまま M1 milestone を起動 (`m1-t1` PLAN.md 挿入は m0.14-t8 tag 設置の前に走った形跡)、以降 M1.5 / M2.0 / M2 と直線進行。**「milestone closure を完了せず次 milestone に進む」pattern**。proc-axis 観点では「milestone 完了基準と PLAN.md status の整合 check が PM workflow に組み込まれてへん」。
- **counter-argument verdict**: for_downgrade (pj-003 と overlap、process-axis では「milestone closure check の workflow 化」として軽量 finding)
- **applicable file**: `agents/loom-pm.md` milestone tag 設置 / 次 milestone 起動 hook
- **proposal**: PM が新 milestone (`/loom-go` で次の Mn+1 起動 OR PLAN.md 編集で新 milestone セクション追加) を起動する前に、**「直前 Mn の完成基準 8 項目 + 全 task `[x]` + tag 設置 を 3 点 cross-check」** を強制。drift 検出時は user に通知 + fast-track close を提案。
- **guidance_proposal**: "新 milestone 起動 (PLAN.md 編集 OR /loom-go) 前に直前 milestone の完成基準を 3 点 check (8 条件達成 / 全 task done mark / git tag 設置)。drift があれば「fast-track close で残 task を片付けてから次 milestone に進む」を user に提案せよ。"

#### finding-proc-005 [for_downgrade / risk: low (originally medium) / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm"]`
- **description**: M0.13 codified **parallel dispatch self-verify discipline** が M2 Task 5+6 / Task 9 で実行された (user context: 「同 message 内 2 / 3 Agent invocation で parallel dispatch ✓、ファイル衝突なし、subagent 間で互いの並列 work を perceive する場面あった」) が、**self-verify の trace** (PM が「並列 dispatch 確認しました、N 体起動」を session log に残す acknowledgement) が retro audit で容易には拾えん。M0.13 で discipline を codify したが、**audit trail の minimum 形式は codify されてへん**。
- **counter-argument verdict**: for_downgrade (実行はされとる、trace 不在は cosmetic、severity medium → low、guidance level 提案)
- **applicable file**: `agents/loom-pm.md` parallel dispatch self-verify section
- **proposal**: parallel batch dispatch 直後の PM 応答に **「parallel dispatch verified: N subagents launched in single message, working_dir=<path>」** の 1 行 audit log を含めよ (機械可読、retro lens が grep で検出可能)。
- **guidance_proposal**: "並列 batch dispatch (Task tool 2+ in single message) 直後、PM 応答冒頭に audit log を 1 行残せ — 形式: `[parallel-batch] N subagents dispatched, working_dir=<path>, batch_id=<retro/task ref>`。retro process-axis lens が並列 verify の実行有無を後で grep 可能にする。"

#### finding-proc-006 [confirm / risk: medium / category: meta-risk-threshold-proposal]

- **target_artifact**: `retro-config`
- **target_agent**: null
- **description**: `~/.claude-loom/user-prefs.json.approval_history` が `{}` のまま、`<project>/.claude-loom/project-prefs.json.last_retro` も 1 件 (`2026-04-29-001`) のみ。**前回 retro が degraded mode で archive markdown のみ生成、approval_history への write は skip** (caveat 通り) → 本 retro でも meta-axis lens が active 履歴データなしで動作する状況、recursive 自己最適化の起動条件未満が継続。これは learned_guidance の use_count 全 0 と連動。
- **counter-argument verdict**: confirm (degraded mode 連続による learning blocker、本 retro 完了時 approval_history 更新を必ず実施すべき)
- **applicable file**: `~/.claude-loom/user-prefs.json` + `<project>/.claude-loom/project-prefs.json`
- **proposal**: 本 retro の user 承認結果を session 終了時に **`~/.claude-loom/user-prefs.json.approval_history.<retro_id>`** に書き込み (approved / rejected / deferred 各 finding ID list)、`project-prefs.json.last_retro` も更新。aggregator agent の責務だが degraded mode 下では retro-pm 自身が write、user 明示承認の上で。
- **guidance_proposal**: null

### researcher (1 件、1 件 drop)

#### finding-res-001 [for_downgrade / risk: low (originally low) / category: researcher-ux-improvement]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: M2 `ui/src/trpc/client.ts:58` に **`const WS_URL = 'ws://localhost:5757/trpc'` literal 直書き**。`localhost` (IPv4/IPv6 dual-stack 解決依存) と SPEC §3.5 / §12 確定値 `127.0.0.1` only の細かな乖離あり。code comment で「M5 polish env-var 化」と既知 deferral だが、SPEC §3.5 binds `127.0.0.1` only と明記しとる以上、UI も `127.0.0.1` literal にした方が SPEC 準拠 (1 char 修正)。production CORS 同 origin 化 (M5) 前の M3 daemon 接続失敗を防ぐ意義もあり。
- **counter-argument verdict**: for_downgrade (M5 polish で env-var 化される予定、低 severity の SPEC sync 改善、`localhost` も実用上問題なし、severity low 維持で「fix anytime」flag)
- **applicable file**: `ui/src/trpc/client.ts:58`
- **proposal**: `const WS_URL = 'ws://127.0.0.1:5757/trpc';` (SPEC §3.5 と同期)。M5 polish で env-var 化までの bridge fix。
- **guidance_proposal**: null

#### finding-res-002 [for_drop]

- **description**: tRPC `wsLink` は `retryDelayMs` callback を内部で利用、claude-loom 側 zustand `connectionStore.handleClose` の attempts++ と二重 reconnection book-keeping の可能性。
- **counter-argument verdict**: for_drop (本 session で tRPC source code 確認できず、外部確証不足、Phase 2 で再評価)

### meta-axis (3 件、0 件 drop)

#### finding-meta-001 [confirm / risk: medium / category: meta-risk-threshold-proposal]

- **target_artifact**: `retro-config`
- **target_agent**: null
- **description**: 本 retro (2026-05-02-001) と前回 (2026-04-29-001) **両方が degraded mode で実行**、approval_history が永続的に空 → meta-axis lens が「category C 連続承認」「lens L rejected_count」を判定する基礎データなし。**recursive 自己最適化機構が起動条件未満で 2 retro 連続停滞**。Task tool 不在問題が解決されん限り meta-axis lens 価値が出ん構造。Task tool 不在 → degraded mode → approval_history 空 → meta-axis idle、の連鎖。
- **counter-argument verdict**: confirm (構造的 blocker 反復、proc-006 と上位下位関係、本 finding は「Task tool 不在問題の上位対処」を求める)
- **applicable file**: 仕組み level — Task tool fallback の 構造的修正 OR retro-pm の degraded-mode-aware approval_history write
- **proposal**:
  1. **短期 (本 retro 内)**: degraded mode でも user 明示承認後の finding は `approval_history.<retro_id>.approved/rejected/deferred` に retro-pm 自身が write (proc-006 と連動)。
  2. **中期 (M3+ daemon 完成後)**: daemon が retro session を kick する場合、daemon 経由で proper Task tool dispatch を保証。CLI orchestrator 経由の retro は Task tool 不在 fallback を持つ。
- **guidance_proposal**: "degraded mode (Task tool 不在) でも user 承認後の findings は必ず approval_history に write せよ。「lens 並列実行できなかった」を理由に approval write を skip すると recursive 学習機構が永久に起動しない。retro-pm 自身が aggregator 不在を補って `~/.claude-loom/user-prefs.json.approval_history.<retro_id>` を直接書き込む、user に明示確認の上で。"

#### finding-meta-002 [for_downgrade / risk: low (originally low) / category: meta-auto-apply-proposal]

- **target_artifact**: `retro-config`
- **target_agent**: null
- **description**: `~/.claude-loom/user-prefs.json.agents.loom-developer.learned_guidance` 1 件 (template `guidance-example-001`) が `active: false`、`<project>/.claude-loom/project-prefs.json.agents.loom-retro-pm.learned_guidance` 1 件 (`guidance-retro-2026-04-29-001-meta`) が `active: true` だが **本 session は本 retro が初回呼出 → use_count: 0 → 1 へ遷移する candidate**。injection 経路 (Customization Layer block 注入) が **本 retro session で初実証**: 本 prompt 内に `[loom-learned-guidance]` block injection は受信したか? — agent definition は `active=true` を `[loom-learned-guidance]` で注入する規定だが、observe しても本 session で injection block の受信 trace が retro-pm 自身の prompt 内で確認できん (system prompt level)。**use_count 自動 increment 機構は M0.11 で「v1 では自動更新せず manual prune」と決まった経緯あり**、これは設計通り、ただし「実際 1 回でも使われたか」が観測不能。
- **counter-argument verdict**: for_downgrade (設計通り manual update、ただし「effect 観測機構」が将来必要、severity low、本 retro ではまだ flag のみ)
- **applicable file**: 観測 logic — `agents/loom-retro-pm.md` (本 agent が injection block 受信を ack する mechanism 検討)
- **proposal**: M0.11.1 (将来 milestone) で **「learned_guidance injection observability」**: agent が `[loom-learned-guidance]` block を受信したら 1 行 ack log (`[learned-guidance-ack] N entries received`) を session 開始応答に含める。retro process-axis lens が injection 実効性を audit 可能。本 retro では flag のみ、構造改修は M0.11.1 で扱う。
- **guidance_proposal**: null

#### finding-meta-003 [confirm / risk: low / category: meta-auto-apply-proposal]

- **target_artifact**: `retro-config`
- **target_agent**: null
- **description**: 前回 retro 2026-04-29-001 の learned_guidance (loom-retro-pm 向け、`active: true`) が「(1) M0.13 codification の実効性 (parallel dispatch self-verify が機能しとるか、TDD red 順序が守られとるか) を check、(2) for_downgrade 4 件の重要度を再評価、して finding 化判断せよ」と指示。**本 retro で実施判定**:
  - **TDD red 順序**: M2 Task 3-9 の git log で各 task が `test(ui): ... RED commit` → `feat(ui): ...` → `docs(plan): mark ...done` の triplet で順序保たれとる ✓ (8 task 全部、timestamps 厳密順)。M0.10/M0.11 で観測された逆転 anomaly は **再発なし**。
  - **parallel dispatch self-verify**: Task 5+6 / Task 9 で実行 (user context)、proc-005 で「trace 不在」を新規 finding 化。実行自体は ✓、audit-trail は要改善。
  - **for_downgrade 4 件 (前回 pj-001 / pj-003 / pj-004 / proc-003)**: pj-001 = 本 retro pj-001 として再上、未適用 (重要度 low 維持)。pj-003 (`/loom-retro-apply` Phase 2 deferred) = 仕様通り未実装、再評価 N/A。pj-004 (worktree 自律 trace) = M2 で worktree 不要だったため検証機会未到来、保留。proc-003 (post-merge fix granularity) = M2 単一機能 milestone のため発火条件未満。
- **counter-argument verdict**: confirm (前回 learned_guidance に従って判定実施、結果を本 retro に反映済、本 finding は「指示通り実行されたこと」の確認 record)
- **applicable file**: 本 retro 自体が指示遂行の record、追加適用なし
- **proposal**: 前回 learned_guidance entry の `active` を **`true` 維持** (継続的 audit 対象として M3 retro でも再 check)、`use_count: 1` に手動更新 (本 retro で参照したこと記録)。
- **guidance_proposal**: null

### user-axis (P2 — Stage 1 公式メンバー)

user 提示の 6 観測を per-process-axis filter で吸収済：

| user 観測 | 吸収先 | category |
|---|---|---|
| GREEN commit handoff anomaly 4 連発 (Tier-1) | proc-001 | process-blocker-pattern |
| Strategy b 切替で 0 anomaly rollback | proc-002 | process-blocker-pattern (Strategy 正式化) |
| parallel dispatch self-verify 実証 (Task 5+6 / Task 9) | proc-005 | process-blocker-pattern (audit trail 改善) |
| inline spec edit + brainstorm-with-spec → plan 圧縮 | (no finding) | M0.13 codified discipline、本 retro でも観測 ✓、改善余地なし |
| TDD red commit 順序 enforcement | meta-003 | M0.13 効果 verify、再発なし |
| reviewer verdict 保存規律 (verdict_evidence 未実施) | proc-003 | process-blocker-pattern |

user findings は全て process / meta lens に正規化された。**本 retro で「user 由来」が独立 finding として残るものなし** (= 既存 lens で漏れなく拾えた = retro architecture 健全性の例証)。

---

## Drop log（archive 用）

| finding | 理由 |
|---|---|
| pj-004 (M2 toast 5 event MVP の SPEC §3.6 event 表との乖離) | M2 plan で M3 deferral 明示済、known scope decision |
| res-002 (tRPC wsLink retry 二重 book-keeping) | 外部確証不足 (degraded mode で source 確認不能)、Phase 2 で再評価 |

---

## Auto-applied（本 retro での自動適用）

該当無し。`auto_apply.categories` は user-prefs / project-prefs 共に空、`max_risk: never` (default)。**全 11 件 user 承認待ち**。

---

## Action items（user 承認後の適用先 mapping）

| finding | 適用先 | 操作 | risk |
|---|---|---|---|
| pj-001 | `PLAN.md` line 4 | `last_synced_at: 1778025600000` (2026-05-02) に更新 | low |
| pj-002 | `README.md` lines 191-213 | M2 frontend 接続例を実装 (`createTRPCReact`) に書き直し OR caveat 追加 | medium |
| pj-003 | `PLAN.md` M0.14 セクション | t6/t7/t8 を closing OR essence 反映済明記 | low |
| proc-001 | `agents/loom-developer.md` Step 8 | commit step 順序明記 + final report に `committed_sha` 必須化 | high |
| proc-002 | `agents/loom-pm.md` + `agents/loom-developer.md` + `docs/COMMIT_GUIDE.md` | Strategy a/b 選択基準明記、`commit_handoff: dev/pm` field 追加 | medium |
| proc-003 | `agents/loom-retro-pm.md` Stage 0 + `agents/loom-pm.md` milestone hook | retro 開始時 verdict_evidence 保存 step 追加 | medium |
| proc-004 | `agents/loom-pm.md` milestone closure hook | 新 milestone 起動前 3 点 cross-check 強制 | low |
| proc-005 | `agents/loom-pm.md` parallel dispatch section | 並列 batch 後の 1 行 audit log 形式 codify | low |
| proc-006 | `~/.claude-loom/user-prefs.json` + `<project>/.claude-loom/project-prefs.json` | 本 retro の approval_history を retro-pm 自身が write | medium |
| res-001 | `ui/src/trpc/client.ts:58` | `localhost` → `127.0.0.1` (1 char、SPEC §3.5 sync) | low |
| meta-001 | `agents/loom-retro-pm.md` degraded mode handling | approval_history write を degraded mode でも実施せよと明記 | medium |
| meta-002 | (M0.11.1 で扱う) | learned_guidance injection observability、本 retro では flag のみ | low |
| meta-003 | `<project>/.claude-loom/project-prefs.json.agents.loom-retro-pm.learned_guidance[0]` | use_count を 0 → 1 に更新、active 維持 | low |

---

## Approval history snapshot (本 retro 終了時更新予定)

```json
{
  "2026-05-02-001": {
    "approved": [],   // user 確認後 finding ID list を埋める
    "rejected": [],
    "deferred": []
  }
}
```

aggregator agent が write する責務だが、degraded mode のため retro-pm 自身が user 明示承認の後に write する (meta-001 finding の趣旨と整合)。

---

## Caveats

1. **`[degraded-protocol]` Task tool 不在**: 本 retro は 4 lens 並列 dispatch / counter-argument / aggregator が独立 subagent として動かず、retro-pm 単体の sequential 4-role 評価。**multi-perspective independence 喪失** (echo chamber risk)、self-counter-argument は同一 agent によるため bias 残存可能性。findings の rigor は通常 retro より低い可能性あり、user は提示時に懐疑度を上げて critique してくれ。
2. **approval_history 永続的に空**: 前回 retro も degraded mode で skip、本 retro の approval write が初の history 蓄積となる予定。proc-006 / meta-001 finding と連動。
3. **researcher lens の external research 制限**: degraded mode で WebSearch / context7 / WebFetch 系を活用せず、SPEC + 実装 内 self-research のみ実施。res-002 の tRPC 二重 retry 検証等は外部確認できんかった。
4. **前回 learned_guidance 確認**: prior retro 2026-04-29-001 の loom-retro-pm 向け guidance (`active: true`) を本 retro で参照し、(1) M0.13 codification 実効性 check、(2) for_downgrade 4 件再評価、を実施 → meta-003 finding として記録。
5. **本 retro 後の M2 完了 hand-off**: PM が次に行う予定 — `m2-complete` tag 設置 + `feat/m2-ui-shell` → main merge。本 retro findings の適用は user 承認後 PM が実施、tag/merge と並行可能。
