# Retro 2026-05-05-001 — M0.11.4 Closure (Phase 1 aesthetic MVP completion)

**retro_id**: 2026-05-05-001
**mode**: conversation
**trigger**: manual (user invoked `/loom-retro` after M0.11.4 closure)
**branch**: main (merge 0be230c 完了直後)
**HEAD**: 0be230c1d74cbe269da95eaef0c520c5c1d9a091
**milestone range**: m0.11.3-complete..m0.11.4-complete (10 commits, 95 files, +7334/-1707)
**scope**: Phase 1 aesthetic MVP closure (M0.11.4 = 3 段階 closure marker の最終段、aesthetic MVP)
**previous retro**: 2026-05-04-001 (M5 + Phase 1 functional MVP closure、F-meta-005 で degraded-mode-synthesis を codify)

---

## Stage 0 status

- `verdict_evidence.json`: built (10 commits, 9 task entry, 4 dev parallel batch を 2 段階記録、reviewer verdict はすべて pass-low)
- `applied_summary.json`: built (前 retro 2026-05-04-001 から 11 件 approved+applied finding 集約、M5 incident で消えた pre-2026-05-04 retro 状態は復元不能)
- HEAD は m0.11.4-complete tag からさらに +1 commit (merge commit 0be230c)、これは hygiene rule (F-proc-004 derived) 通り tag 直後に main へ flush した結果

## Synthesis mode

**degraded-mode-synthesis**: Task tool unavailable in this session, retro-pm performed 4-lens synthesis itself. 通常の echo-chamber 抑制 (4 並列 lens + counter-arguer 別 agent) は本 session で適用されず、retro-pm 単一視点である。F-meta-005 (前 retro で codify) の **2 連続発生**。findings の confidence は通常 retro より低めに評価すべき。

---

## Findings

### F-pj-001 — F-001 (project-settings dropdown data binding) carryover の structural fix
- lens: pj-axis | severity: MEDIUM | proposal_type: structural | risk: medium
- 状況: t19 smoke で F-001 (medium) が捕捉、t20 で「aesthetic MVP block せず carryover」処理。production code で /project-settings が dropdown label/option を render してない (regression)。Phase C t16 で RPG style 化した際の data binding 落ち。tag は設置済だが、user 視点で「3 段階 MVP 完成」と謳いながら 11 route 中 1 route が機能 broken の状態は **aesthetic MVP completeness assertion の信頼性を損なう**。
- root cause 候補: t16 で SessionList + TokenMeter + ProjectSettings + Sidebar + Header を 1 dev (dev-D) が unified batch で書直した際の cognitive load、ProjectSettings の dropdown spec がほかの 3 view より厚く、testing も assertion が弱かった。
- **action**: F-001 の structural fix を Phase 2 entry の 1 件目 task として優先実施。代替: aesthetic MVP completion marker (m0.11.4-complete) の README 注釈に「F-001 carryover あり」と明示、tag rollback はしない (Phase 1 dogfood phase の pragmatic 判断)。

### F-pj-002 — REQ-045 verification gate completion 不在
- lens: pj-axis | severity: LOW | proposal_type: structural | risk: low
- 状況: M0.11.4 spec phase で REQ-045 (aesthetic MVP completion verification) を tests/REQUIREMENTS.md に追加した上で実装したが、**REQ-045 を assert する自動 test が存在しない** (smoke skill の手動実行で代替、但し報告だけ)。SPEC §3.6.12.6 で `m0.11.4-complete` を「aesthetic MVP completion」と定義するが、何をもって「達成」とするかの **machine-checkable assertion** は loom-ui-smoke skill が aesthetic verify report を出した = 達成、と緩く運用。
- **action**: REQ-045 を「smoke skill が full scope で `routes_failed === 0` で完了 = REQ-045 PASS」に明文化、もしくは smoke skill 実行を aesthetic milestone closure 時の必須 gate として codify。Phase 2 で再評価可。

### F-pj-003 — Phaser dependency 物理削除の rollback evidence 整備
- lens: pj-axis | severity: LOW | proposal_type: record-only | risk: low
- 状況: t17 で Phaser dependency 物理削除 (M3.0 infra retire)。SPEC §3.6.9.1 + §12 で「α-1 → α-2 (rollback)」と注記済だが、PLAN.md の M3.0 milestone task 履歴は touch されとらん (M3.0 task は当時 done で凍結、M0.11.4 で実装内容が反転)。retro feedback loop (M3.0 → M0.11.4) は機能、但し過去 milestone への retroactive 注記が PLAN.md に不在。
- **action**: 記録のみ。PLAN.md M3.0 section 末尾に「M0.11.4 t17 で Phaser dependency rollback、§3.6.9.1 archive 値化」note 追加 candidate。Phase 2 で実施可。

### F-proc-001 — PM 統合 commit handoff (M0.14 codification) の 9 commit 連続成功 record
- lens: process-axis | severity: RECORD | proposal_type: structural-record | risk: low
- 状況: M0.14 で codify した「commit_handoff=pm」(dev verdict=pass → PM 統合 commit) 戦略が、M0.11.4 で **9 連続 commit 成功** (Phase A 1 + Phase B 3 + Phase C 1 + Phase D 4)。dev は test pass + reviewer verdict=pass まで、commit は PM が統合実施、で全ての impl commit が unified annotation default で書かれた。
- **action**: 記録のみ。SPEC §3.6.8.6 の unified default rule (前 retro で reverse) + commit_handoff=pm rule (M0.14) の **2 つの structural codification の組合せ effectiveness** を retro feedback loop の成功例として PLAN.md or SPEC §3 preamble に reference。

### F-proc-002 — proc-002 path C self-review 全 9 commit 適用 record (Task tool degraded mode の 2 連続証言)
- lens: process-axis × meta-axis | severity: HIGH | proposal_type: structural | risk: medium
- 状況: 前 retro F-proc-001/F-meta-004 で「Task tool degraded mode persistence、proc-002 path C 未定義」を HIGH で codify。本 milestone でも **全 9 impl commit が path C self-review** (Task tool deferred 状態継続)。本 retro session 自身も Task tool unavailable で degraded-mode-synthesis (F-meta-005 codify 済の現象が 2 連続)。proc-002 path C は機能している (M0.11.4 で 9/9 commit pass、blocker 無し) が、**Task tool 復旧 ETA 不在** + **path C と通常 reviewer の精度差を測る metric 不在**。
- 構造的観点: F-meta-005 codify で「degraded mode = 透明化さえすれば運用可」が前 retro 結論だが、(a) 2 milestone 連続で degraded mode = persistent state、(b) 本 retro 自身も Task tool 不在で 4 lens 並列 echo-chamber 抑制 working できず、 = path C は **永続化された運用 mode** に近い。
- **action**: 二択。(a) Task tool 復旧を Phase 2 entry condition に明記、復旧不能なら path C を **default mode** に昇格 (proc-002 を恒久化)、(b) path C 精度を測る lightweight metric (例: 2 milestone 後の retro で path C commits の reviewer follow-up rate を集計) を導入。priority: HIGH。

### F-proc-003 — Phase B Stage B1+B2 各 4 dev parallel batch の負荷 / efficiency 観察
- lens: process-axis | severity: LOW | proposal_type: record-only | risk: low
- 状況: M0.11.4 は **過去最大規模の dev parallel batch を 2 段階連続** (Stage B1 = 4 dev / 129 tests added、Stage B2 = 4 dev / 99 tests added、Stage C = 4 dev / 1734 insertions)。dispatch 戦略で foundation (Stage B1) → consumers (Stage B2) → orchestration (B3) の依存階層を仕切ったため、Stage B1 SSoT freeze (CatSprite + ROSTER + Phase A CSS class) が Stage B2 の前提として全 4 dev に直参照された。1 commit / 4 dev の unified PM 統合 commit が成立。
- **action**: 記録のみ。PLAN.md or SPEC §3.6.8 に「依存階層を持つ大規模 batch は SSoT freeze stage を仕切ると 4 dev parallel が成立する」を retro outcome として note。Phase 2 milestone 設計時の参考。

### F-proc-004 — milestone tag → main flush hygiene の 2 連続成功 record
- lens: process-axis | severity: RECORD | proposal_type: structural-record | risk: low
- 状況: 前 retro F-proc-004 で「milestone tag 設置 = PR opening trigger」を codify、本 milestone で **chain depth 1 + tag 直後 main flush** を実行 (m0.11.4-complete tag → 即 merge 0be230c)。前 retro で 4-branch chain × 32+ commits の hygiene gap を解消した直後の 1st validation。
- **action**: 記録のみ。CLAUDE.md hygiene rule の effectiveness 評価 success record として retro feedback loop に reference。

### F-meta-001 — loom-ui-smoke skill 初の milestone closure 用途 1st validation
- lens: meta-axis | severity: RECORD | proposal_type: structural-record | risk: low
- 状況: M0.11.3 で構築した loom-ui-smoke skill が M0.11.4 t19 で初の milestone closure verification 用途で full scope 実行。**11 route 中 1 件 medium finding (F-001) を捕捉**、automated test (782 PASS) で漏れた production data binding regression を実機 browser で検出。skill effectiveness は確認 (「automated test green ≠ 画面が動く」gap を構造的に塞ぐ役割を果たした)。
- 検出力: 1 medium / 1 info / 1 low / 11 route。false positive 比率は info / low が「監視継続」処理で finding 化されたが、user 認知負荷を増やす程度ではない。
- **action**: 記録のみ。Phase 2 で UI 関連 milestone の closure verification として skill 標準化 candidate。

### F-meta-002 — degraded-mode-synthesis 2 連続発生、retro 自身の echo-chamber 抑制機構の運用継続 gap
- lens: meta-axis | severity: HIGH | proposal_type: structural | risk: medium
- 状況: 前 retro 2026-05-04-001 で F-meta-005 として codify した「retro-pm degraded mode synthesis」が、**本 retro session でも再発** (Task tool unavailable で 4 lens 並列 dispatch 不能、retro-pm 自前 synthesis)。前 retro F-meta-005 の action は「agents/loom-retro-pm.md or SPEC §3.9.x に明示」(structural)、これは前 retro で approved → applied されとる (`apply_history` は空だが `applied_in: 6d9b36b`)。にも関わらず再発、= **codification しても Task tool 復旧しない限り degraded mode 永続化** が事実。
- 構造的観点: F-proc-002 と表裏一体。retro 自体の echo-chamber suppression infra (4 lens parallel + counter-arguer separation) が 2 連続で稼働せず、= claude-loom の retro 機能の **core invariant の 1 つが継続的に違反状態**。symptomatic patch (degraded tag 透明化) で運用継続できとるが、retro finding の confidence が構造的に下がる状態が milestone を跨いで持続。
- **action**: 二択 user 判断。(a) Task tool 復旧を Phase 2 entry の **HARD blocker** に格上げ (復旧してから次 milestone)、(b) **path C を retro lens dispatch にも適用** = retro-pm が 4 lens を逐次的に self-execute する protocol を SPEC §3.9.x に正式化 (degraded mode を 2nd-class じゃなく 1st-class option に昇格)、独立 lens の echo-chamber 抑制効果は失われるが、現実運用との整合性は確保。priority: HIGH。

### F-meta-003 — Phase 1 → Phase 2 boundary entry criteria 整備 gap
- lens: meta-axis | severity: MEDIUM | proposal_type: structural | risk: medium
- 状況: README は「M0.11.4 完了 (Phase 1 MVP — functional + verification + aesthetic completed)」と明示するが、PLAN.md / SPEC.md に **Phase 2 entry criteria の formal list が未整備**。前 retro F-meta-001 で「dogfood pattern Phase 1 effectiveness 1st validation」を recorded、F-pj-001 で「Phase 1 closure PR」を applied 済だが、Phase 2 entry の checklist / blocker / scope 定義 (M1 daemon 導入、frontend-design 連携、daemon multi-project, etc.) が散在。
- **action**: PLAN.md (or SPEC §3 preamble) に「Phase 2 entry criteria」section 新設、Phase 1 retro carryover (F-meta-002 の Task tool 復旧 / F-001 の structural fix / F-meta-002 path C 1st-class 昇格 等) を blocker として整理。Phase 2 milestone 着手前。

### F-meta-004 — auto_apply.categories 空 = applied_summary build state が信用に値する観察
- lens: meta-axis | severity: LOW | proposal_type: record-only | risk: low
- 状況: user-prefs.json `auto_apply.categories: []` + `max_risk: "never"` で運用継続、本 retro でも全 finding を user 提示 → 個別承認の運用予定。前 retro F-meta-002 で「applied_summary graceful skip is symptomatic」を applied (commit 6d9b36b)、本 retro では正常に prior session 1 件 (2026-05-04-001) からの 11 件 approved+applied を集約成功。M0.11.1 の lifecycle tracking 機構が 1 milestone を跨いで安定運用。
- **action**: 記録のみ。

### F-res-001 — Phaser → DOM/SVG 移行で得た知見の external 還元 candidate
- lens: researcher | severity: LOW | proposal_type: record-only | risk: low
- 状況: M3.0 で Phaser 4 React mount infra を構築 → M0.11.4 で DOM/SVG (`<svg viewBox>` + `<rect shapeRendering="crispEdges">` + `image-rendering: pixelated`) に移行。「pixel-perfect rendering を library 依存ゼロで実現」は claude-blog-skill 等の sibling project にも還元可能。
- **action**: 記録のみ。Phase 2 以降で claude-blog-skill 側の retro skill が独自に検討。

### F-res-002 — claude-room-handoff design bundle full port pattern の external 還元 candidate
- lens: researcher | severity: LOW | proposal_type: record-only | risk: low
- 状況: M0.11.4 は Claude Design tool export bundle (`claude-room-handoff.zip`) を Component port matrix (15 view + 共通 components) で逐次 port、**13 view 全面書直しを 4 dev parallel batch で 1 milestone 内完遂**。design SSoT (jsx + tokens.css + styles.css) を Phase A SSoT freeze → Phase B/C consumer parallel の 2 段階で運用するパターンが機能。
- **action**: 記録のみ。external project (claude-blog-skill 等) の design import 時の reference pattern として archive。

### F-res-003 — Token meter empty state UX (F-002 carryover) の structural 化候補
- lens: researcher | severity: LOW | proposal_type: record-only | risk: low
- 状況: t19 smoke で F-002 (info) として「TokenMeterView empty state で active fill 不在」が捕捉。前 retro F-res-003 で「Token meter polling UX iteration」を applied (d041b6e) 済だが、empty state visual 強化は当時 scope 外。
- **action**: 記録のみ。Phase 2 token meter UX iteration の backlog に登録、structural fix は不要 (current 状態で機能阻害なし)。

---

## Cross-axis correlations

- **F-pj-001 + F-meta-001**: F-001 を smoke skill が捕捉した事実 = skill effectiveness の証明、F-001 自身の fix は別問題。両方を同時整理。
- **F-proc-002 + F-meta-002**: Task tool degraded mode が dev workflow (proc-002 path C) と retro workflow (degraded-mode-synthesis) で **同じ root cause** を持つ。SPEC §3.6.8 + §3.9.x で **統一 protocol** 化 candidate。
- **F-pj-002 + F-meta-001**: REQ-045 の machine-checkable assertion 不在 + smoke skill が完了 report 自体を生成 → REQ-045 を smoke skill output に bind すれば自動 closure verification 化可能。
- **F-meta-003 + F-meta-002 + F-proc-002**: Phase 2 entry criteria に Task tool 復旧を blocker として組み込むか、path C 1st-class 化を組み込むか、user 判断必須。

## Priority for user presentation

1. **HIGH structural** (Phase 2 entry 前): F-proc-002, F-meta-002 (両方 Task tool 持続 degraded mode の表裏)
2. **MEDIUM structural** (Phase 2 entry 前): F-pj-001, F-meta-003
3. **LOW structural**: F-pj-002
4. **RECORD only**: F-proc-001, F-proc-004, F-meta-001
5. **record-only / LOW**: F-pj-003, F-proc-003, F-meta-004, F-res-001, F-res-002, F-res-003

総数: 13 findings (5 structural + 8 record-only / low、HIGH 2、MEDIUM 2、LOW 9)

---

## degraded-mode-synthesis disclosure

本 retro は Task tool unavailable のため retro-pm が 4 lens synthesis + counter-argument + aggregation を自前実施。通常の echo-chamber 抑制 (4 並列 lens + counter-arguer 別 agent + aggregator 別 agent) は本 session で適用されず、retro-pm 単一視点の synthesis である点を user に明示。findings の confidence は通常 retro より低めに評価すべき。

これは前 retro F-meta-005 で codify した degraded-mode-synthesis protocol の **2 連続適用**、F-meta-002 の structural finding として本 retro で再提起。
