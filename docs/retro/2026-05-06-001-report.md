# Retro Report: 2026-05-06-001

- **retro_id**: 2026-05-06-001
- **milestone**: m0.11.5-complete (Phase 1 closure trinity 1/3)
- **mode**: conversation → apply (path C / degraded mode)
- **synthesis_mode**: degraded-mode-synthesis
- **range**: m0.11.4-complete .. m0.11.5-complete
- **scope**: M0.11.5 Lazy Daemon Auto-Launch Implementation (9 task / 6 dev dispatch)
- **trigger**: milestone-hook + user `/loom-retro` 起動
- **final findings**: 18 件 (16 lens + F-USER-001 critical + F-USER-002 high)
- **all-accept**: 全 18 件 approved + applied (path C / retro-pm self-apply、Task tool deferred)

## Stage 0 build

- `verdict_evidence.json` v2 schema、6 dev dispatch + PM closure 1 件 = 7 task entry、全 verdict pass、全 self_review_path C
- `applied_summary.json` v2 schema、過去 12 件 (2026-05-04-001) + 2026-05-05-001 aggregate marker 1 件 + 個別 1 件 = 14 件
- 既存 2026-05-05-001 applied_summary は v1.0.0 文字列形式、本 retro で v2 整合 (F-pj-004 / F-meta-002 で structural fix 提案 + 採用)

## Stage 1 (degraded mode self-synthesis)

通常 4 lens 並列 dispatch を行うが、Task tool 利用不可のため retro-pm 自身が pj-axis / process-axis / meta-axis / researcher の役割を sequential self-synthesis。user-input lens (PM 観察 5 件) は 4 lens findings に absorb (`user_input_origin: true` label 付与)。

## Stage 2 counter-argument (self-impose)

各 finding に self counter-argument を回し、3 件を downgrade / aggregate (F-pj-003 → record-only / F-proc-005 → F-proc-001 aggregate / F-res-002 → record-only)、drop ゼロ。

## Stage 3 final findings (18 件、F-USER-001/002 含む)

| id | lens | category | severity | type | applied_in |
|----|------|----------|----------|------|-----------|
| F-pj-001 | pj-axis | spec-drift-architectural | medium | structural | e8da3c4 |
| F-pj-002 | pj-axis | spec-drift-architectural | high | structural | e8da3c4 |
| F-pj-003 | pj-axis | spec-drift-doc-update | low | record-only | (history only) |
| F-pj-004 | pj-axis | spec-drift-architectural | high | structural | e8da3c4 |
| F-proc-001 | process-axis | process-discipline-success-record | informational | record-only | (history only) |
| F-proc-002 | process-axis | process-tdd-violation | low | structural | 0dba074 |
| F-proc-003 | process-axis | spec-drift-architectural | high | structural | e8da3c4 |
| F-proc-004 | process-axis | spec-drift-architectural (※ blocker-pattern→architectural reclassify) | high | structural | e8da3c4 |
| F-meta-001 | meta-axis | meta-auto-apply-proposal | high | structural | e8da3c4 |
| F-meta-002 | meta-axis | meta-auto-apply-proposal | medium | structural | e8da3c4 |
| F-meta-003 | meta-axis | meta-auto-apply-proposal | medium | structural | e8da3c4 |
| F-meta-004 | meta-axis | meta-auto-apply-proposal | informational | record-only | (history only) |
| F-meta-005 | meta-axis | meta-auto-apply-proposal | medium | structural→record-only (downgrade) | (downgrade rationale below) |
| F-res-001 | researcher | researcher-ux-improvement | medium | structural | c9723bc |
| F-res-002 | researcher | researcher-ux-improvement | informational | record-only | (history only) |
| F-res-003 | researcher | researcher-ux-improvement | informational | record-only | (history only) |
| **F-USER-001** | user-axis | install-fix-critical | critical | structural | b79f419 (hotfix) |
| **F-USER-002** | user-axis | process-discipline-codify | high | structural | 1b9712f |

### F-pj-001 [structural / medium]: REQ ID 採番 protocol 未文書化 (applied: e8da3c4)

t6 が `REQ-XXX` placeholder、t7 が `REQ-045` 採用、M0.11.4 で REQ-045 が "aesthetic MVP completion verification" として既使用の疑い。`tests/REQUIREMENTS.md` SSoT との整合確認が loom-developer agent の TDD red phase pre-check に組込まれていない。

**Structural fix applied**: `agents/loom-developer.md` TDD Workflow Step 3 に "REQ ID 採番 pre-check" 新設、`tests/REQUIREMENTS.md` SSoT collision check (`grep -E "^- \*\*REQ-NNN\b" tests/REQUIREMENTS.md`) を必須化、REQ-XXX placeholder 禁止。本 retro 自身の hotfix で REQ-046 (daemon.js symlink bootstrap) を初例として採番、collision check pass 確認済み。

### F-pj-002 [structural / high]: pending.json apply_history back-fill 責務 SSoT 不在 (applied: e8da3c4)

2026-05-05-001 の 14 finding 全採用 (commit ffd3848) されたが pending.json の applied_in / apply_history field が null/[] のまま。機械的 applied_summary build が壊れる。Phase 2 で予定の `/loom-retro-apply` v1 未実装ゆえ、apply commit 時の pending.json update protocol が SPEC 上 missing。

**Structural fix applied**: SPEC §3.9.11 末尾に "apply commit 時の back-fill 責務" SSoT 章追加、in-session apply (loom-retro-pm 主体) / out-of-session apply (apply 実行主体の手動 back-fill) の 2 path codify。schema_version v2 維持規律 + back-fill 検証 (Stage 0 lazy build 時 WARN log) も明記。

### F-pj-003 [record-only / informational]: README + DOC_CONSISTENCY_CHECKLIST reminder

M0.11.5 で auto_launch default true 化 / hooks/loom-launch-ui.sh / 7 種 slash command 拡張、README 反映 (t8) は完了。`docs/DOC_CONSISTENCY_CHECKLIST.md` 通過で総点検が望ましい (M4 で自動化予定の手作業)。

**Apply**: reminder のみ、`approval_history.spec-drift-doc-update` 加算 (2 → 3)。本 retro の SPEC/PLAN 編集 13 箇所相当を含むため、retro session 後に DOC_CONSISTENCY_CHECKLIST 手動 run 推奨。

### F-pj-004 [structural / high]: schema_version SSoT 統一規律不在 (applied: e8da3c4)

retro 系 file 間で 3 種混在: user-prefs v1 (integer 1) / retro pending v2 (integer 2) / 過去 applied_summary v1.0.0 (文字列)。

**Structural fix applied**: SPEC §6.9.6.1 新設、schema_version SSoT 統一表組明記 (user-prefs v1 / project-prefs v1 / retro pending v2 / applied_summary v2 / verdict_evidence v2、過去 v1.0.0 文字列 file は leave-as-is policy)。§6.9.7 applied_summary schema を `z.literal(1)` → `z.literal(2)` 統一。format 規律 (semver 文字列禁止 / integer 必須) 明記。

### F-proc-001 [record-only / informational]: M0.11.5 success record + branch hygiene 遵守

全 9 task 完走 + 6 dev dispatch 全 verdict pass + parallel batch 3 dev concurrent 初成功 + branch hygiene rule 遵守 (milestone tag = PR opening trigger 規律) のポジティブ累積記録。

**Apply**: `approval_history.process-discipline-success-record` 加算 (9 → 10)、SPEC 改変なし。

### F-proc-002 [structural / low]: t4 GREEN+REFACTOR 統合 commit sub-variant SPEC 明記 (applied: 0dba074)

t4 dev (commit 8087071) で GREEN+REFACTOR を 1 commit 統合、TDD red 順序遵守は維持されとるが agent definition の RED→GREEN→REFACTOR 別 commit ガイドからの minor deviation。

**Structural fix applied**: SPEC §3.6.8.6 末尾に "Strategy a sub-variant: 2-commit RED→GREEN+REFACTOR" block 追加。acceptable 条件 (REFACTOR trivial + GREEN と同一 unit + `[GREEN+REFACTOR squashed]` annotation) と 3-commit strict mode 維持条件を codify。

### F-proc-003 [structural / high]: path C default 昇格 (6/6 evidence) (applied: e8da3c4)

本 session 6/6 dev dispatch 全部 path C self-review (Task tool deferred) で pass、Strategy b default 反転 (M0.14) と同じ累積 evidence パターン。

**Structural fix applied**: SPEC §3.6.8.7 で path C を default 反転 (Reviewer dispatch triple path)。`agents/loom-developer.md` Step 9 で path C を先頭並替 + Step 9 開始時に Task tool 利用可能性 probe 必須化 (`ToolSearch select:Task` 空結果 → degraded mode 自動 enter)。M0.14 Strategy b 反転と同 pattern。

### F-proc-004 [structural / high]: pre-existing failures carryover escalation rule (applied: e8da3c4)

3 件 (docs_release / dry_run_applied_summary / m1_docs) が M0.X 累積 carryover、3 retro 連続未解決。

**Structural fix applied**: SPEC §3.9.14 新設 carryover escalation rule (3 retro 連続 deferred → 専用 fix milestone insert 必須)。PLAN.md に M0.X-test-debt-cleanup milestone 新設 (5 task、Phase 2 entry 前 完走必要)、carryover escalation 第 1 例として codify。

### F-meta-001 [structural / high]: degraded mode probe 強制化 + persistence escalation (applied: e8da3c4)

本 session で 2 retro 連続 degraded mode、Task tool 利用可否 probe protocol が agent definition に未組込、復旧条件 / 復旧確認 hook も SPEC 未定義。

**Structural fix applied**: SPEC §3.9.13 拡張、Stage 0 直前 `ToolSearch select:Task` 必須化 + degraded mode 持続 3 retro 連続 → §3.9.13 review 必須 escalation rule 追加。本 retro が 2 連続目、次 retro が degraded で 3 連続成立した場合に SPEC 改訂が triggered される。

### F-meta-002 [structural / medium]: applied_summary schema_version v2 整合 (F-pj-004 と pair) (applied: e8da3c4)

F-pj-004 と統合採用、SPEC §6.9.6.1 + §6.9.7 で v2 (integer) 統一済。

### F-meta-003 [structural / medium]: apply_history back-fill 責務 (F-pj-002 と pair) (applied: e8da3c4)

F-pj-002 と統合採用、SPEC §3.9.11 末尾に back-fill 責務 SSoT 化済。Phase 2 で `/loom-retro-apply` 実装スコープに「apply 時 source pending.json apply_history back-fill 必須化」が組込予定。

### F-meta-004 [record-only / informational]: M0.11.5 dispatch 戦略 record

parallel batch 3 dev concurrent 初成功 + Strategy b PM 統合 commit (M0.14 codification) の本格運用 2 回目成功、harness self-improvement の dogfood 検証成立。

**Apply**: `approval_history.meta-auto-apply-proposal` 加算 (11 → 12)、SPEC 改変なし。

### F-meta-005 [structural→record-only downgrade]: personality preset directory bootstrap (root cause re-analysis)

#### 元 finding: install.sh に personality preset directory bootstrap step 追加

`~/.claude/prompts/personalities/` directory 不在で Customization Layer M0.9 personality preset IO が空振り、と分析。

#### Counter-argument deep dive (apply phase verification)

apply phase で実態確認した結果：

- claude-loom リポ内 `prompts/personalities/` に **4 preset 既同梱済** (default / friendly-mentor / strict-drill / detective)
- `install.sh` line 109 で `prompts/` directory を `~/.claude/prompts/` に **symlink 配線済** (REQ-023 で test PASS 確認済み)
- 本 user 環境で `~/.claude/prompts/personalities/` 不在の真因は「**install.sh が走っとらん user 環境特有 state**」であり、structural fix 不要

agent definition (§3.6.5 + agents/loom-pm.md 既存) には「file not found → warn user + use default」fallback も codify 済、user 環境の install 走り抜け case は完全 covered。

#### Apply 判定: structural → record-only downgrade

structural fix (templates/personalities/ 新設 + install.sh 配線追加) は **redundant**、SPEC 改変不要。`approval_history.meta-auto-apply-proposal` 加算 (12 → 13、record-only 扱い)。

**学び (record)**: retro lens の root cause analysis が user 環境 state を考慮せず誤読する pattern。next retro で「実態確認 → analysis 検証 step を Stage 1 lens に追加」検討候補 (F-meta-005 自身が next retro の input findings 候補に変身)。

### F-res-001 [structural / medium]: loom-ui-smoke skill scope 拡張 plan (Phase 2) (applied: c9723bc)

M0.11.5 lazy daemon auto-launch + 7 種 slash command 拡張で daemon trigger + browser open + WebSocket event 流通の e2e UX が Phase 2 検証スコープ。

**Structural fix applied**: `skills/loom-ui-smoke/SKILL.md` に "Phase 2 carryover" section 追加、daemon auto-launch trigger / headless detection / LOOM_NO_UI override / clipboard copy / warm-start path / install.sh dependency audit pair の 6 項目を Phase 2 entry milestone で skill scope 拡張する旨記録。

### F-res-002 [record-only / informational]: 公開後 community feedback 取込導線 (premature, defer)

PR #1 公開準備済、現時点では premature、Phase 2 entry 後に再評価。

**Apply**: `approval_history.researcher-ux-improvement` 加算 (9 → 10)。

### F-res-003 [record-only / informational]: Task tool deferred Claude SDK 制約観察

F-meta-001 と pair、observation record のみ。

**Apply**: `approval_history.researcher-ux-improvement` 加算 (10 → 11)。

### F-USER-001 [structural / critical]: install.sh daemon symlink bootstrap missing (applied: e2f1815 RED + b79f419 GREEN)

#### Finding (user input lens)

`hooks/loom-launch-ui.sh:18` が `DAEMON_BIN=$HOME/.claude-loom/daemon.js` を参照しとるが、`install.sh` line 5 comment「M1 以降で daemon ビルド配置を追加予定」と書きつつ実際 daemon symlink 配線が install.sh に **未実装**。ユーザー初回 install 時に `~/.claude-loom/daemon.js` 不在、cold-start path で `start_daemon` が `return 1` → daemon launch しない構造的 critical issue。SPEC §3.2 lazy daemon auto-launch flow が機能不全。

#### Counter-argument deep dive (verdict: confirm + critical 維持)

- **実態確認**: `loom-launch-ui.sh:18` `DAEMON_BIN="${LOOM_DAEMON_BIN:-$HOME/.claude-loom/daemon.js}"` 参照、`daemon/dist/index.js` build artifact 存在、`install.sh` 全文 scan で daemon symlink 配線 step 不在を verify
- **影響範囲**: M0.11.5 で auto_launch default true 化 + lazy daemon flow 整備 + 7 種 slash command 拡張済、しかし install.sh bootstrap 不在で **新 default が初回 install 後に silent failure** する構造的 critical
- **F-USER-002 との関係**: 本 finding が露呈した構造 risk class を closure 前に塞ぐ structural codification = F-USER-002 (dependency audit on default change)、pair の structural fix

#### Hotfix applied (本 retro 内で実施、SPEC §3.9.x P4 結果)

- **REQ-046 採番** (REQ-045 までの最終 + 1、F-pj-001 collision check 通過)、`tests/REQUIREMENTS.md` に M0.11.5 hotfix section 新設
- **RED phase** (e2f1815): `tests/install_test.sh` に LOOM_HOME 上書き sandbox + `~/.claude-loom/daemon.js` symlink 検証 assertion 追加、`daemon/dist/index.js` 不在環境では SKIP (build 前 environment 配慮)
- **GREEN phase** (b79f419): `install.sh` に LOOM_HOME 環境変数追加 + `daemon/dist/index.js → $LOOM_HOME/daemon.js` symlink を install 末尾に追加 + 既存 file 衝突時エラー終了 (REQ-004 / REQ-009 同 pattern) + `daemon/dist/index.js` 不在時 warning + skip (idempotent)
- **検証**: `bash tests/install_test.sh` で REQ-046 PASS 確認 (`/var/folders/.../daemon.js -> daemon/dist/index.js` symlink 配線確認)、既存 9 REQ assertion 全 PASS 維持

### F-USER-002 [structural / high]: dependency audit on default change codify (applied: 1b9712f)

#### Finding (user input lens)

F-USER-001 が露呈した構造 risk class (default 値変更時に依存 install / config / runtime pipeline step が partial implementation で silent failure を生む) を **process discipline として codify** すべき。F-USER-001 hotfix だけでは同 pattern の class 再発を防げない、dependency audit を milestone closure 前 必須 check として組込む。

#### Counter-argument deep dive (verdict: confirm + high 維持、symptomatic patch ではなく structural)

- F-USER-001 single fix は症状対処 (P4 違反候補)、本 finding が **structural alternative**
- audit checklist 4 step (install / config / runtime / rollback) は機械的に走らせ可、低運用負荷
- M0.11.5 と同 pattern のリスクが Phase 2 default 反転系 milestone で再発する可能性高い (auto-prune default / auto-apply default / コードベース watch default 等)

#### Structural fix applied

- **SPEC §3.6.8.8 新設** (1b9712f): trigger 検出条件 (default 反転 / runtime path active 化 / hook bootstrap 必須化) + audit 4 step checklist + rationale (M0.11.5 case 引用) codify
- **CLAUDE.md "Dependency audit 規律"** 追加 (Branch hygiene 規律 直後に並置)、4 step audit + rationale codify
- **agents/loom-pm.md "Dependency audit on default change" workflow 新設** (Milestone retro hook 直後)、5 step workflow (trigger 検出 → audit 4 step → 失敗 block / pass tag 設置) + rationale codify

---

## Apply state (final)

全 18 件 approved + applied (state: `approved` + `applied_in: <commit_sha>`)、drop ゼロ。

| commit SHA | scope | finding ids closed |
|---|---|---|
| e2f1815 | F-USER-001 RED phase (test assertion) | F-USER-001 (RED) |
| b79f419 | F-USER-001 GREEN phase (install.sh hotfix) | F-USER-001 (GREEN) |
| 1b9712f | F-USER-002 dependency audit codify | F-USER-002 |
| e8da3c4 | spec-drift bulk (8 finding structural) | F-pj-001, F-pj-002, F-pj-004, F-proc-003, F-proc-004, F-meta-001, F-meta-002, F-meta-003 |
| 0dba074 | F-proc-002 Strategy a sub-variant codify | F-proc-002 |
| c9723bc | F-res-001 Phase 2 carryover note (loom-ui-smoke) | F-res-001 |
| (no commit、archive only) | record-only history (approval_history) | F-pj-003, F-proc-001, F-meta-004, F-meta-005 (downgrade), F-res-002, F-res-003 |

**total**: 6 commit + 6 record-only history = 18 finding all-applied。

## degraded-mode-synthesis disclosure

**本 retro session は degraded mode synthesis で実施された (SPEC §3.9.13 protocol)**。

- **理由**: Task tool が利用不可 (top-level tool list 不在 + ToolSearch deferred 一覧にも非掲載)、4 lens parallel dispatch + counter-arguer + aggregator の 6 体 subagent dispatch が impossible
- **影響**:
  - 4 lens の独立 critique による echo-chamber 抑制が低下、retro-pm 単一視点の bias 持込みリスクあり
  - confidence は通常 retro より低めに評価される (high severity finding でも user 判断時の double-check を推奨)
- **mitigation**:
  - 全 finding に `degraded_mode_synthesis: true` field 付与済み
  - counter-argument self-impose を全 18 件に対して明示記述 (Stage 2 で 3 件を downgrade / aggregate + F-USER-001 deep dive で root cause 確認 + F-meta-005 で structural → record-only re-analysis)
  - PM 観察 5 件 (`user_input_origin: true`) を独立 input として absorb、user perspective 反映
- **2 retro 連続 degraded mode**: 2026-05-05-001 + 2026-05-06-001 で 2 連続、F-meta-001 で structural escalation rule (3 retro 連続 → SPEC §3.9.13 review 必須) を提案 + 採用済 (SPEC §3.9.13 拡張)。本 retro が **3 連続前の警告 retro** に当たる
- **復旧条件**: Task tool が top-level tool list (system prompt) に出現 → 通常 4 lens parallel dispatch 復帰、agent definition の Stage 0 probe で自動判定

## Apply path C disclosure

本 retro の **apply phase 自体も path C** で執行された (Task tool deferred、retro-pm が全 file write を自前実施、Strategy a 相当の retro apply path 規約)。

- 6 commit + 1 archive rewrite = 7 commit を retro-pm が直接 commit (commit_handoff: dev、retro-pm 自身)
- branch: `docs/retro-2026-05-06-001-apply` (新規切出、main 直 commit 禁止 hygiene 遵守)
- F-USER-001 hotfix は RED → GREEN 2-commit (SPEC §3.6.8.6 strict mode、install path の critical scope ゆえ TDD audit 性最優先)
- F-USER-002 / spec-drift bulk / F-proc-002 / F-res-001 は SPEC/PLAN/agents/skills doc edit 系で 1 commit/finding-cluster (docs scope、unified annotation 不要)
