# Retro 2026-05-06-003 — M0.X-runtime-mode-recovery milestone

- **retro_id**: 2026-05-06-003
- **project**: claude-loom
- **milestone**: m0.x-runtime-mode-recovery-complete (+ post-tag Bug A hotfix)
- **scope**: spec phase + 4-way parallel impl batch + closure + post-tag Bug A hotfix を包括的に振り返る
- **synthesis_mode**: degraded-mode-synthesis
- **escalation_status**: 4-strike-continuation (3-strike-trigger-1st は 2026-05-06-002 で達成済、本 retro で 4 retro 連続持続)
- **generated_at**: 2026-05-06

## Trigger
manual — user 承認で起動。PM が PR opening trigger + milestone retro hook の流れで提案、user "OK そうしよか" で起動。

## Milestone scope (12 commits)
| commit | type | task |
|---|---|---|
| 92e01e7 | docs(spec) | SPEC §3.2.1/§3.2.2 daemon mode + /mode endpoint codify |
| 06d340f | docs(plan) | M0.X-runtime-mode-recovery + M0.X-hook-ingest-recovery 追加 |
| f43006a | feat(daemon) | /mode endpoint + LOOM_DEV_MODE 判定 [RED+GREEN unified] (t2/t3) |
| e95dbf5 | docs(plan) | t2/t3 done + t9 scope refinement |
| 1ce1b11 | feat(hooks) | /mode probe + Vite redirect in loom-launch-ui (t4) |
| 3a24688 | feat(install) | post-install stale tsx watch detection (t8) |
| d3aa805 | feat(daemon) | pnpm dev preflight + LOOM_DEV_MODE auto-inject (t5/t6) |
| 6e09df5 | feat(hooks,commands) | /loom-stop --all zombie cleanup (t7) |
| ab0428c | docs(plan) | t4-t9 done (parallel batch + scope absorption) |
| 5afef01 | docs(claude,readme) | SPEC §3.2.1/§3.2.2 cascade (t10) |
| f0eeadb | test(e2e) | daemon runtime mode integrated e2e (t11) |
| 91cdcbb | fix(hooks) | start_daemon port bind verify gap (Bug A hotfix) |

## Stage summary

- **Stage 0 (前提整備)**: retro_id 2026-05-06-003 採番、verdict_evidence + applied_summary build (前 retro F-USER-005..008 + F-USER-001..004 + F-pj/proc/meta/res 系列の applied 状態保持)、command_frequency.log 不在 confirm (空 tally で proceed)
- **Stage 1 (4 lens degraded synthesis)**: retro-pm 自前で sequential synthesis、17 件 initial findings (pj 4 + proc 5 + meta 4 + res 4) generate
- **Stage 2 (counter-argument self-pass)**: retro-pm 自前で反証検査、`for_drop` 0 件 / `for_downgrade` 8 件 (うち merge 2 件, refine 5 件, record-only 縮退 1 件) / `confirm` 9 件
- **Stage 3 (aggregator synthesis)**: 12 件 distinct findings に整理 (initial 17 → merge 2 + record-only 縮退 5 + refine 5 = 12)

## Findings (12 件)

### F-USER-009 — trust recovery milestone series 構造化 (Layer 2.5 codify)
- **lens**: user-pj | **category**: spec-drift-architectural | **risk**: critical | **proposal_type**: structural
- **summary**: F-USER-005/006 + F-USER-007/008 + Bug A の 3 連続発覚 pattern は全て user 直接 dogfood verify でしか発覚せず、Layer 1 (bash + vitest + e2e) と Layer 2 (browser smoke) は両方 pass していた構造的 gap。SPEC §10.4 に Layer 2.5 (PM dogfood smoke) を milestone closure default の必須 step として追加する。
- **structural_fix**:
  1. SPEC §10.4 に Layer 2.5 (PM dogfood smoke) subsection 新設: tag 設置直前に PM が actual user fixture (loom-launch-ui.sh 起動 → daemon URL access → /mode probe → UI 表示確認) を Bash + WebFetch で実機 verify
  2. agents/loom-pm.md milestone closure workflow に Layer 2.5 step 組込
  3. Layer 3 (user verify request) は record only として SPEC §10.4 末尾に補助 layer 化 — 開発側の dogfood gap を user 拘束で塞ぐ運用は anti-pattern として明記

### F-pj-001 — post-tag hotfix codification (hybrid SSoT)
- **lens**: pj-axis | **category**: spec-drift-architectural | **risk**: high | **proposal_type**: structural
- **summary**: F-USER-007/008 hotfix (commit c31a88e) + Bug A hotfix (commit 91cdcbb) が 2 連続で milestone tag 設置後に同 branch に追加された運用 pattern を codify。tag 移動禁止 + 当該 milestone retro scope への必須 inclusion を SSoT 化する hybrid。
- **structural_fix**:
  1. SPEC §3.6.8.x に 'post-tag hotfix protocol' 新設: tag 移動禁止、commit message に [post-tag-hotfix] annotation 必須、当該 milestone retro scope に強制 include
  2. agents/loom-pm.md milestone closure workflow に rule 追加
  3. 本 retro 自身を初適用 case として archive 記録 (本 archive がそれ)

### F-pj-002 — boot health-check polling SSoT
- **lens**: pj-axis | **category**: spec-drift-architectural | **risk**: medium | **proposal_type**: structural
- **summary**: Bug A hotfix で実装した start_daemon /health polling 詳細仕様 (max_attempts 5 × 0.5s + EADDRINUSE message + env override) が SPEC §3.2 step 4 で言及あるのみで SSoT 化されとらん。
- **structural_fix**:
  1. SPEC §3.2 に 'Boot health-check polling' subsection 新設
  2. tests/REQUIREMENTS.md REQ-055 cross-reference 追加

### F-proc-001 — 4-way parallel batch dogfood validation 成功記録
- **lens**: process-axis | **category**: process-discipline-success-record | **risk**: low | **proposal_type**: record-only
- **summary**: PM が file disjoint pre-audit → unified-with-annotation で 4 commit 同時生成、CLAUDE.md F-proc-001 規律 + agents/loom-pm.md Step 3 規律が機能した dogfood validation 成功。但し同 batch が Bug A trigger でもあった (F-proc-002 と pair)。

### F-proc-002 — parallel batch + SessionStart hook 多重発火 interaction
- **lens**: process-axis | **category**: process-blocker-pattern | **risk**: medium | **proposal_type**: structural
- **summary**: 各 subagent dispatch で SessionStart hook 多重発火 → daemon に POST /event spam → daemon 一時 busy → /health probe 失敗 silent → false cold-start trigger → open_browser 多重 invoke が Bug A の symptom chain。M0.X-hook-ingest-recovery (next milestone) の POST /event spam fix と root cause overlap。
- **structural_fix**:
  1. CLAUDE.md parallel batch section に注意喚起 1 行追加
  2. M0.X-hook-ingest-recovery scope に 'parallel batch interaction test fixture' 追加
  3. SPEC §3.6.8 parallel batch 章に side-effect note 追加

### F-proc-003 — TDD path C 6 連続 success record
- **lens**: process-axis | **category**: process-discipline-success-record | **risk**: low | **proposal_type**: record-only
- **summary**: 本 milestone 6 連続適用 (t2/t3 + t4 + t5+t6 + t7 + t8 + t11 + Bug A hotfix)、累積 12+ 連続 pass。SPEC §3.6.8.7 path C default 反転 (2026-05-06) の運用 evidence 累積。

### F-proc-004 — post-tag hotfix の TDD discipline 遵守記録
- **lens**: process-axis | **category**: process-discipline-success-record | **risk**: low | **proposal_type**: record-only
- **summary**: Bug A hotfix で REQ-055 + Test A/B/C + 既存 test fixture 更新が遂行された。F-USER-007/008 hotfix と合わせ post-tag hotfix で TDD bypass されない 2 連続 evidence。

### F-meta-001 — degraded mode 4-strike continuation transparency
- **lens**: meta-axis | **category**: meta-auto-apply-proposal | **risk**: high | **proposal_type**: structural
- **summary**: 3-strike trigger は 2026-05-06-002 達成済、本 retro で 4 連続。追加 escalation rule は YAGNI risk だが status field の transparency 追加が価値あり。
- **structural_fix**:
  1. SPEC §3.9.13 に 'persistence count tracking' field 追加: N-strike-continuation form を 4 以降も継続記録
  2. user-prefs.json retro_session_history に反映
  3. Task tool upstream research 進捗確認 (本 retro action item)、N-strike continuation 6+ で blocker promotion 検討の future trigger codify

### F-meta-002 — Phase 2 Entry Checklist 拡張
- **lens**: meta-axis | **category**: meta-auto-apply-proposal | **risk**: medium | **proposal_type**: structural
- **summary**: trust recovery milestone series 3 連続 + post-tag hotfix 2 連続を Phase 2 entry HARD blocker 候補に codify。Phase 2 multi-contributor 環境では single dev dogfood に依存できない、Layer 2.5 SSoT 化 + 運用 evidence 蓄積が必須。
- **structural_fix**:
  1. PLAN.md Phase 2 Entry Checklist に 'Layer 2.5 PM dogfood smoke 運用 N 回 success record' 追加
  2. post-tag hotfix 0 件 milestone N 回連続 record を Phase 2 Entry soft blocker 候補

### F-meta-003 — learned_patterns 空 silent gap (record-only)
- **lens**: meta-axis | **category**: meta-auto-apply-proposal | **risk**: low | **proposal_type**: record-only
- **summary**: project-prefs.json learned_patterns が空のまま、4 retro 連続 degraded mode で aggregator write 経路 audit 不能。次回 normal-mode retro (Task tool 復旧時) で write logic audit が必要。

### F-meta-004 — command-frequency.log 不在 silent failure
- **lens**: meta-axis | **category**: process-blocker-pattern | **risk**: medium | **proposal_type**: structural
- **summary**: F-USER-004 (commit f124bff) で post_tool hook probe 配線済の認識だったが log file 不在。post_tool hook 未発火 or path 乖離 silent failure 可能性、M0.X-hook-ingest-recovery investigation scope に明示追加。
- **structural_fix**:
  1. M0.X-hook-ingest-recovery PLAN scope に 'post_tool freq probe 動作 verify task' 追加
  2. SPEC §3.9.15 を log 不在時の retro-pm side warning explicit に refine

### F-res-001 — Bug A 副次改善 candidates (Phase 2 record-only)
- **lens**: researcher | **category**: researcher-ux-improvement | **risk**: low | **proposal_type**: record-only
- **summary**: open <url> tab 集約 UX (既存タブ再利用)、exponential backoff polling (250ms→500ms→1s→2s)、Task tool upstream research 進捗確認の 3 candidate を Phase 2 record-only として archive。

---

## degraded-mode-synthesis disclosure

- **synthesis_mode**: degraded-mode-synthesis
- **echo-chamber risk acknowledge**: 本 retro は Task tool 利用不能 (deferred listing にも static functions list にも `Task` 不在) のため、retro-pm が 4 lens (pj/process/meta/researcher) を sequential に self-synthesize し、counter-argument pass も retro-pm 自身が実施した。通常 protocol の「4 並列 lens + 別 agent counter-arguer」による echo-chamber 抑制が適用されとらん。findings の confidence は通常 retro より一段低めに評価する。
- **3-strike escalation status**: 4-strike-continuation (2026-05-05-001 / 2026-05-06-001 / 2026-05-06-002 / 本 retro 2026-05-06-003 で 4 連続持続)。3-strike trigger 後の必須 action items (SPEC §3.9.13.1) の 5 項目は前 retro で全実行済、本 retro は status transparency record + 必要なら escalation rule 拡張提案 (F-meta-001) に focus。
- **F-USER-009 critical priority**: 3 連続 trust recovery milestone series + post-tag hotfix 2 連続の発覚 pattern は echo-chamber risk を超えた **factual signal** であり、degraded mode synthesis でも confidence 高評価。Phase 2 entry blocker に格上げ提案 (F-meta-002 と pair)。
- **本 archive 公開後の audit hint**: 本 retro 採用 finding が次 retro (Task tool 復旧後) で normal-mode synthesis を経由した時、本 archive と finding gap を比較することで degraded mode synthesis の bias 評価が可能になる。
