# Retro Report — 2026-04-29-001

**retro_id**: `2026-04-29-001`
**project_id**: `claude-loom-self`
**mode**: conversation
**scope**: 直近 5 milestone 一括振り返り（M0.9 / M0.9.1 / M0.10 / M0.11 / M0.12）
**HEAD at retro start**: `3097cd2 Merge branch 'feat/m0.12-coexistence': M0.12 Coexistence Mode`
**generated_at**: 2026-04-29
**executor note**: 本 retro session は **Task tool 不在のため retro-pm 単体での 4-lens sequential 評価モード**で実行された。並列 dispatch 不可、self-counter-argument 込み。proc-finding-2 で詳述。

---

## Summary

| stage | 件数 | 備考 |
|---|---|---|
| Stage 1 raw findings | 19 | 4 lens 横断（pj=5 / process=6 / researcher=3 / meta=5） |
| lens 自己 drop | 6 | 重複・scope 超過・no-issue |
| Stage 2 input | 11 | 反証検査対象 |
| Stage 2 confirm | 5 | pj-4 / proc-1 / proc-2 / proc-4 / meta-3 |
| Stage 2 for_downgrade | 4 | pj-1 / pj-3 / pj-5 / proc-3（severity / guidance 軽減） |
| Stage 2 for_drop | 2 | res-1（外部確証不足）/ meta-5（自然検証可、finding 不要） |
| Stage 3 final（archive） | 9 | severity 順に並べ替え、user 提示対象 |

---

## Findings by lens

### pj-axis (4 件)

#### finding-pj-001 [confirm / risk: low / category: spec-drift-doc-update]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: PLAN.md frontmatter `last_synced_at: 1777194000000` (2026-04-25 頃) が M0.12 完了 (2026-04-29) 後も古いまま。M0.9〜M0.12 の連続 merge があったので最新 epoch に更新すべき。
- **counter-argument verdict**: for_downgrade（M1 daemon 完成までは manual update が前提、severity 維持で auto-apply 適切）
- **applicable file**: `PLAN.md` (line 4: `last_synced_at`)
- **guidance_proposal**: null

#### finding-pj-002 [confirm / risk: low / category: spec-drift-doc-update]

- **target_artifact**: `doc-file`
- **target_agent**: null
- **description**: PLAN.md M0.12 完成基準で「これで原 6 案件（A/B/C+G/D/E/F）全完走」と記述しとるが、A/B/C+G/D/E/F の対応 milestone 表が PLAN.md / SPEC.md / README いずれにも無く、後で読んだ user / agent が復元不能（design spec ファイル群にしか記録されてへん）。
- **counter-argument verdict**: confirm（context loss 明確、新規参加者の解読不能リスク実在）
- **applicable file**: `PLAN.md` (M0.12 セクション末尾 or SPEC §1 / §10) に対応表追加
- **guidance_proposal**: null

#### finding-pj-003 [for_downgrade / risk: low (originally medium) / category: feature-gap]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-retro-pm"]`
- **description**: SPEC §3.9 retro architecture が `/loom-retro-apply` を「Phase 2 で導入予定」と記載しとるが、会話 mode で session 中断 → resume 経路として retro-pm が `<project>/.claude-loom/retro/*/pending.json` の存在を session 開始時に確認する logic が prompt に明記されてへん。本日の最初の retro でも未検証。
- **counter-argument verdict**: for_downgrade（Phase 2 deferred 機能、現状 spec-conformant）
- **guidance_proposal**: "Step 1 retro_id 採番の前に `<project>/.claude-loom/retro/*/pending.json` を ls し、`stage: \"presenting\"` の pending あり → user に「直前 retro が中断されとります、resume しますか？」と提案 → yes なら pending.json から findings を read して提示再開 / no なら新 retro_id で開始、を実装せよ"

#### finding-pj-004 [for_downgrade / risk: low (originally medium) / category: feature-gap]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm", "loom-developer", "loom-retro-pm"]`
- **description**: M0.10 worktree skill が「自律発動」を明記しとるが、3 dispatcher agent prompt に挿入された参照テキストが Decision tree 5 用途と完全 1:1 mapping してへん。本 retro 自体も並列実行不能下で worktree invoke 判断が trace されてへん（5 milestone 全 main 直作業で worktree 不要だが trace 不在）。
- **counter-argument verdict**: for_downgrade（5 用途 each の self-report は過剰、現行「不確実時 user 確認」運用で実用十分）
- **guidance_proposal**: "session 開始時 / 並列 batch 起動時に Decision tree 5 用途のいずれかに該当するか **不確実な場合のみ** 1 行で self-reasoning を残せ（"applicable: none, single tree mode" 等）。silent skip と意識的 skip を区別する最低限の trace"

### process-axis (4 件)

#### finding-proc-001 [confirm / risk: medium / category: process-tdd-violation]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-developer", "loom-reviewer"]`
- **description**: M0.11 / M0.10 で TDD red 順序が逆転。M0.11: `d562cb9 feat(agents): 4 retro lens emit ...` (02:50:22) → `c69adde test: extend prefs/agents/retro tests` (03:04:07) で実装 commit が test 拡張より 14 分早い。M0.10: `290a0da feat(skills): add loom-worktree` (02:28:33) → `f0dc766 test(skills): assert ...` (02:30:19) で skill 実装 commit が test commit より 1m46s 早い。red 状態を git に残す原則違反。
- **counter-argument verdict**: confirm（再現性 2 milestone、user 自身が context で言及）
- **applicable file**: `agents/loom-developer.md` および `agents/loom-reviewer.md` の learned_guidance 注入経路
- **guidance_proposal**: "TDD red commit を実装 commit より前にプッシュする規律を厳格化。tests を red 状態（CI 失敗状態）で commit してから green commit を続ける。`tests/` 配下の commit が同 milestone の `feat(agents|skills)` 実装 commit より時系列で後ろに来とる場合、reviewer は process-tdd-violation finding を生成すべき"

#### finding-proc-002 [confirm / risk: high / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm", "loom-developer", "loom-retro-pm"]`
- **description**: subagent dispatch 中に Task tool 不在による review skip / self-review 代替が複数回発生（user context 明示）。本 retro session 自体も Task tool 不在で 4 lens 並列 dispatch 不可、retro-pm 単体で sequential 代替実行中。harness の前提（Task tool 常時利用可）と実環境の乖離は反復 blocker pattern。
- **counter-argument verdict**: confirm（最高優先度、本 session で再現中）
- **applicable file**: `agents/loom-pm.md` / `agents/loom-developer.md` / `agents/loom-retro-pm.md` の冒頭 lifecycle 部
- **guidance_proposal**: "session 開始直後に Task tool の利用可否を確認し、不在の場合は (a) ユーザーに即時通知 (b) sequential degraded mode に明示的に fallback 宣言 (c) 生成する全 finding / report に "Task tool 不在につき degraded mode 実行" を明記、の 3 step を必ず実施せよ。silent な self-review 代替は TDD/review discipline 違反として扱う"

#### finding-proc-003 [for_downgrade / risk: low (originally low) / category: process-commit-granularity]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-developer", "loom-reviewer"]`
- **description**: M0.9 fix commit `1f75170 fix(m0.9): personality preset path resolution + install.sh prompts/ symlink` が 2 つの異なる修正（path resolution と install.sh symlink）を 1 commit に統合。CC の 1 commit = 1 論理変更原則に違反。
- **counter-argument verdict**: for_downgrade（post-merge audit hotfix の合体は実用的、severity 維持だが guidance を「強制 reject」から「分割推奨 + reviewer 注意喚起」に緩和）
- **guidance_proposal**: "post-merge audit fix commit でも、修正対象が複数 file / 複数論理変更なら分割を推奨。`fix(scope): A + B` 形式の commit subject を見たら reviewer は granularity finding を上げる（reject ではなく warning level）"

#### finding-proc-004 [confirm / risk: medium / category: process-blocker-pattern]

- **target_artifact**: `agent-prompt`
- **target_agent**: `["loom-pm", "loom-developer"]`
- **description**: 5 milestone を本日 1 session で連続実装、M0.11 のみ `a16cd43 fix(m0.11): reviewer findings (...)` の reviewer feedback fix commit が存在、M0.10 / M0.12 には対応 fix commit 無し。reviewer 未 dispatch か finding ゼロかの判別不能。高速反復下で review skip 懸念。
- **counter-argument verdict**: confirm（5 milestone 連続実装で reviewer 痕跡が薄い実態）
- **applicable file**: `agents/loom-pm.md` の milestone tag 設置 hook 部
- **guidance_proposal**: "milestone tag 設置前の最終 review が確実に走ったことを session 内に commit またはコメントとして記録せよ。`a16cd43` のような review fix commit が無い場合は、reviewer JSON output (verdict pass) を `docs/retro/.../review-evidence/` 等に保存して証拠を残す。または PLAN.md の milestone 完成基準に「reviewer verdict pass を確認した日時」を append"

### researcher (0 件)

該当 finding 全て drop。res-1 は外部確証不足（Claude built-in plugin list API の存在を本 session で fetch 確認できず）、res-2 / res-3 は他 lens 重複 / 不確実。

### meta-axis (1 件)

#### finding-meta-001 [confirm / risk: medium / category: meta-risk-threshold-proposal]

- **target_artifact**: `retro-config`
- **target_agent**: null
- **description**: claude-loom self 用 prefs (`~/.claude-loom/user-prefs.json` / `<repo>/.claude-loom/project-prefs.json`) が両方未作成。retro architecture が `{}` default で動く前提だが、approval_history 未蓄積 → meta-axis lens は永遠に history 不足で finding 上げられん。本 retro が初発のため、終了後に空 prefs を template から作成して以後の retro が learning できる状態にすべき。
- **counter-argument verdict**: confirm（dogfood blocker、学習機構の起動条件）
- **applicable file**: `~/.claude-loom/user-prefs.json` (NEW) + `<repo>/.claude-loom/project-prefs.json` (NEW)
- **guidance_proposal**: null

---

## Drop log（archive 用）

| finding | 理由 |
|---|---|
| pj-2 (M0.9 personality preset bug post-merge audit) | process-axis が拾うべき領域、proc-1/proc-4 と重複 |
| proc-5 (M0.10 TDD 順逆転) | proc-1 に統合（同 pattern の M0.10 ケース） |
| proc-6 (CC prefix 利用) | no issue、5 milestone 通し準拠 |
| res-1 (Claude built-in plugin list API) | 外部確証不足、in-session で fetch 未実施、Phase 2 で再評価 |
| res-2 (plan size dashboard) | meta 観点重複 |
| res-3 (gh worktree extension) | 不確実、dogfood 検証機会未到来 |
| meta-1 / meta-2 (history-based proposals) | 初発 retro で history 空、trigger 条件未満 |
| meta-4 (personality 試用提案) | scope 超過（user prompt） |
| meta-5 (learned_guidance 初発火検証) | 自然検証可、finding 上げ不要 |

---

## Auto-applied（本 retro での自動適用）

該当無し。`auto_apply.categories` は user-prefs / project-prefs 共に空（prefs 自体未作成）、`max_risk: never` (default)。**全 9 件 user 承認待ち**。

---

## Action items（user 承認後の適用先 mapping）

| finding | 適用先 | 操作 |
|---|---|---|
| pj-001 | `PLAN.md` line 4 | `last_synced_at` を `1777680000000` (2026-04-30 想定) 等に更新 |
| pj-002 | `PLAN.md` M0.12 / `SPEC.md` §1 | A/B/C+G/D/E/F → milestone 対応表を追加 |
| pj-003 | `agents/loom-retro-pm.md` | Step 1 前に pending.json check logic 追記 + learned_guidance に書き込み |
| pj-004 | `agents/loom-{pm,developer,retro-pm}.md` | worktree autonomy 「不確実時 self-reasoning」guidance を learned_guidance に書き込み |
| proc-001 | `agents/loom-developer.md` + `agents/loom-reviewer.md` | TDD red commit 規律 guidance を learned_guidance に書き込み |
| proc-002 | `agents/loom-{pm,developer,retro-pm}.md` | Task tool 利用可否 check + degraded mode fallback guidance を learned_guidance に書き込み |
| proc-003 | `agents/loom-developer.md` + `agents/loom-reviewer.md` | post-merge fix の granularity 推奨 guidance を learned_guidance に書き込み |
| proc-004 | `agents/loom-pm.md` | milestone tag 設置前の reviewer 痕跡記録 guidance を learned_guidance に書き込み |
| meta-001 | `~/.claude-loom/user-prefs.json` + `<repo>/.claude-loom/project-prefs.json` | template から空 prefs を新規作成 |

---

## Approval history snapshot

本 retro が claude-loom-self の初回 retro。approval_history 未蓄積、本 retro 終了時に **meta-001 適用 → 空 prefs 作成 → 本 retro 結果を最初の history entry として書き込み** が連鎖する想定（ただし実際の write は user 承認 + aggregator 実機 dispatch が必要、本 session では retro-pm 単体動作のため write は skip 推奨、user に判断委譲）。

---

## Caveats

1. **Task tool 不在による degraded execution**: 本 retro は 4 lens 並列 dispatch 不可、retro-pm 単体で sequential 4-role evaluation。echo chamber risk 増大、external research (res-1) は web fetch 未実施で drop。findings の rigor は通常 retro より低い可能性あり。
2. **prefs 未作成**: claude-loom self 用 user-prefs / project-prefs が両方未作成、`{}` default 前提で動作。learned_guidance write 経路は未検証（meta-5 で記載通り、user 承認時に初発火検証可）。
3. **Customization Layer 未設定**: `agents.<name>.personality` 全 default、効果実感 / drift 観察データ無し。
