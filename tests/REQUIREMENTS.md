# claude-loom Acceptance Requirements

> 本ファイルは ID 付き受入要件の一覧（claude-blog-skill 流儀）。
> 各 REQ-XXX は対応する test ファイルでカバーされる。

## M0: Dev Harness

- **REQ-001**: `install.sh` 実行で `~/.claude/agents/loom-*.md` のシンボリックリンクが設置される
- **REQ-002**: `install.sh` 実行で `~/.claude/commands/loom-*.md` のシンボリックリンクが設置される
- **REQ-003**: `install.sh` を 2 回実行しても破壊的変更を起こさない（idempotent）
- **REQ-004**: `~/.claude/agents/loom-*.md` に通常ファイルが既存の場合、`install.sh` はエラー終了する
- **REQ-005**: 各 `agents/*.md` ファイルが valid な YAML frontmatter を持つ（name / description フィールド必須）
- **REQ-006**: 各 `commands/*.md` ファイルが valid な YAML frontmatter を持つ（description フィールド必須）
- **REQ-007**: 全 agent 定義の name フィールドが「loom-」プレフィックスで始まる

## M0.5: Approval-Reduction Skills

- **REQ-008**: `install.sh` 実行で `~/.claude/skills/loom-*/` のディレクトリシンボリックリンクが設置される
- **REQ-009**: `~/.claude/skills/loom-*/` が通常ディレクトリ（symlink でない実体）として既存の場合、`install.sh` はエラー終了する
- **REQ-010**: 各 `skills/loom-*/SKILL.md` ファイルが valid な YAML frontmatter を持つ（name / description フィールド必須）
- **REQ-011**: 全 skill 定義の name フィールドが「loom-」プレフィックスで始まる

## M0.7: Conventional Commits + GitHub Flow

- **REQ-012**: `templates/claude-loom/project.json.template` の `rules.commit_prefixes` に CC 11 種すべて（feat / fix / docs / style / refactor / perf / test / build / ci / chore / revert）が含まれる
- **REQ-013**: `templates/claude-loom/project.json.template` の `rules.branch_types` に GitHub Flow 用 10 種すべて（feat / fix / docs / style / refactor / perf / test / build / ci / chore）が含まれる
- **REQ-014**: `docs/COMMIT_GUIDE.md` が存在し、空でない（CC + GitHub Flow ガイドの SSoT）

## M0.8: Retro Architecture

- **REQ-015**: `skills/loom-retro/SKILL.md` が valid な YAML frontmatter（既存 `skills_test.sh` でカバー、新規 test 不要）
- **REQ-016**: 7 つの `agents/loom-retro-*.md` が valid frontmatter + `loom-` prefix（既存 `agents_test.sh` でカバー）
- **REQ-017**: `templates/user-prefs.json.template` と `templates/project-prefs.json.template` が jq empty で valid + `schema_version: 1` フィールド存在
- **REQ-018**: `docs/RETRO_GUIDE.md` が存在 + 非空 + 4 lens 名（pj-axis / process-axis / researcher / meta-axis）言及あり + category enum 列挙あり

## M0.9: Harness Polish

- **REQ-019**: `agents.*` schema validation in user-prefs / project-prefs（jq empty 通る + precedence project > user > frontmatter）
- **REQ-020**: 4 personality preset files が `prompts/personalities/{default,friendly-mentor,strict-drill,detective}.md` に存在、本文非空（default は空 OK）
- **REQ-021**: `agents/loom-developer.md` および全 reviewer agent prompt が `docs/CODING_PRINCIPLES.md` を参照（grep で検証）
- **REQ-022**: 全 13 agent prompt に Customization Layer 参照記述あり（top-level: self-read 指示、dispatched: `[loom-customization]` block 読取指示、dispatcher: 注入指示）。新規 skill `loom-write-plan` / `loom-debug` が valid frontmatter + 必須 section
- **REQ-023**: `install.sh` は `prompts/personalities/` を `~/.claude/prompts/personalities/` に symlink、agent prompt は `~/.claude/prompts/personalities/<preset>.md` 絶対パスで参照（user 他 PJ で claude-loom インストール時に preset md にアクセス可）

## M0.10: git worktree 統合

- **REQ-024**: `skills/loom-worktree/SKILL.md` が valid frontmatter + 必須 6 sections（When to use / Decision tree / Commands / Path convention / Safety rules / Anti-patterns）、`commands/loom-worktree.md` が valid frontmatter、`templates/project-prefs.json.template` に `worktree` section（base_path / auto_cleanup / max_concurrent）含み jq empty で valid、3 agent (loom-pm / loom-developer / loom-retro-pm) prompt が `loom-worktree` を参照。

## M0.11: retro → agent prompt feedback loop

- **REQ-025**: retro lens 4 体 (pj-judge / process-judge / meta-judge / researcher) が finding 出力に `target_artifact` / `target_agent` / `guidance_proposal` field を含む（agent-prompt 行きの場合必須）。`loom-retro-aggregator` が承認 finding を `agents.<target>.learned_guidance[]` (project-prefs default、user 昇格 opt-in) に書き込み。13 agent prompt が `learned_guidance` を read し `[loom-learned-guidance]` block として注入（top-level: self-read、dispatched: dispatcher 注入）。`templates/{user,project}-prefs.json.template` の `agents.<name>.learned_guidance: []` が jq empty で valid。

## M0.12: Coexistence Mode

- **REQ-026**: `templates/claude-loom/project.json.template` に `rules.coexistence_mode` (enum `"full|coexist|custom"`、default `"full"`) + `rules.enabled_features` (array<string>、default `["all"]`) 追加、jq empty で valid。3 dispatcher agent (`agents/loom-pm.md` / `loom-developer.md` / `loom-retro-pm.md`) prompt が `coexistence_mode` を read し runtime gate（feature group 不在なら該当機能 skip）を実装。`commands/loom-mode.md` 新設で mode 切替可能。

## M0.13: Retro Discipline & Process Hardening

- **REQ-027**: `docs/RETRO_GUIDE.md` に retro 基本方針 P1/P2/P3 (自己 + PJ 改善 / user 参加 / action plan 化) 明記、4 retro lens prompt が `freeform-improvement` category instruction を含む（generic 禁止、concrete file/commit 参照必須）、`agents/loom-retro-aggregator.md` の output に action plan セクション必須記述、`agents/loom-pm.md` に parallel dispatch self-verify + Task tool fallback degraded mode + inline spec edit + doc batch 並列化 + reviewer verdict 保存記述、`agents/loom-developer.md` に TDD red commit 時系列 enforcement。`./tests/run_tests.sh` で 8 PASS 維持。

## M1: Daemon + Hooks Foundation

- **REQ-028**: `install.sh` が `hooks/*.sh` を `~/.claude/hooks/` に symlink + `~/.claude/settings.json` に hooks 5 種配線（jq + atomic mv）。`bash install.sh` 後に Claude Code が hook event を daemon に POST 可能。

## M1.5: UI Prep Backend

- **REQ-029**: M0.8-M0.13 feature の UI 要件 (SCREEN_REQUIREMENTS Q1-Q6) に対応する 6 tRPC router (`retro` / `prefs` / `personality` / `worktree` / `coexistence` / `discipline`) を `daemon/src/routes/` に追加、`AppRouter` type に wire-up。`events` router に 3 subscription (`onLearnedGuidanceChange` / `onWorktreeChange` / `onDisciplineMetricUpdate`) 追加。各 router は最低 2 procedure (list + 主要 mutation) + zod schema、`pnpm --filter @claude-loom/daemon test` で全 PASS。frontend (M2) から `import type { AppRouter } from "@claude-loom/daemon"` で 6 router の型推論可能。

## M2: UI Shell

- **REQ-030**: prototype design (`ui/prototype/`) を Vite + React 18 + TS strict + Tailwind の proper 実装に port、tRPC `wsLink` で daemon に接続、vertical slice 1-2 画面で live data pipeline 確立。`pnpm --filter @claude-loom/ui build` 成功 + `pnpm --filter @claude-loom/ui dev` で 9 view (Room / CharSheet / Gantt / Plan / Retro / Worktree / Consistency / Customization / LearnedGuidance) が react-router v6 navigate で表示。AppShell は persistent Room layer + 半透明 panel routing で Phaser 3 mount は M3 へ defer。tokens.css は rgb 形式で `tailwind.config.ts` の variable extend 経由参照、3 theme (`data-theme="pop|dusk|night"`) 切替で root variable 上書き動作。WS 切断時 exponential backoff (1s → 2s → 4s → … → 30s cap) で再接続 + ConnectionBanner 表示 + toast 5 event (`daemon_disconnected` / `daemon_reconnected` / `consistency_finding_new` / `subagent_failed` / `project_added`) 動作。zustand `connectionStore` が `wsLink` の `onOpen` / `onClose` / `onError` callback を bridge。vertical slice として Room の `agentList` subscription または Plan の `planItems` query が daemon → UI まで live で流通。`pnpm --filter @claude-loom/ui test` で全 PASS (visual snapshot + WS retry + toast の 3 種)。

## M2.1: M3-prep Cleanup（verdict_evidence + M0.14 closure）

- **REQ-031**: SPEC §3.9.10（verdict_evidence 概念 + write timing）+ §6.9.5（zod 完全 schema）に整合する形で `agents/loom-retro-pm.md` Stage 0 に verdict_evidence lazy build 5 step（git log → task_id 推定 → transcript reviewer JSON 抽出 → PM hint reference 優先使用 → zod schema validate + file write）記述。`agents/loom-pm.md` milestone tag hook に `[reviewer-dispatch-refs]` block 形式（`task_id=<id>, commit_sha=<sha>, reviewer_agent=<name>, review_mode=<single|trio>` の 1 行 N entries）記述、PM 自身は file write しない（責務分離）。`docs/RETRO_GUIDE.md` に verdict_evidence 運用 SSoT 章追加（lazy build + PM hint + audit 用途）。`docs/DOC_CONSISTENCY_CHECKLIST.md` に M2.1 セクション追加 + M0.13 line 125 の `verdict 保存 hook` 記述を §3.9.10 / §6.9.5 参照に update + M0.14 t6 cleanup 統合（M0.14 t6 closure）。`tests/agents_test.sh` に (a) `agents/loom-retro-pm.md` Stage 0 verdict_evidence build step assertion (b) `agents/loom-pm.md` milestone hook の `[reviewer-dispatch-refs]` block 記述 assertion (c) `agents/loom-retro-process-judge.md` の 3 新 category (`process-permission-friction` / `process-routine-automation-opportunity` / `process-keybind-opportunity`) schema assertion (M0.14 t7 closure) 追加。`./tests/run_tests.sh` で **12 PASS** 維持（既存 12 test files の green 状態維持、agents_test.sh 内 sub-assertion 3 種拡張）。`tag m2.1-complete` 設置、`m0`〜`m2-complete` 全保持。PLAN.md M0.14 セクションに「t6/t7 essence は M2.1 で統合実施、`m0.14-complete` 別 tag 不在のまま PLAN-SSoT 整合化」注記済（pj-003 closure）。

## M3.0: Room View（Phaser mount + ピクセル世界観）

- **REQ-032**: SPEC §3.6.9.1（Phaser 3 React 内 mount = 自前 `useEffect` + `useRef`）に整合する形で `ui/src/views/RoomView/` に Phaser game instance mount component を実装。`useEffect(() => { gameRef.current = new Phaser.Game(config); return () => gameRef.current?.destroy() }, [])` lifecycle、HMR `import.meta.hot.dispose` で `game.destroy()` で memory leak ゼロ。tile map JSON + tokens.css `var(--color-bg)` 連携で 3 theme (pop/dusk/night) 切替動作。Agent sprite が `agent_id ↔ sprite 1:1 mapping` で daemon `agent` subscription event を受けて状態 (idle/busy/失敗) 切替アニメ動作。vertical slice として 1 PJ の agent_pool 全 sprite が Room View に live 表示。`pnpm --filter @claude-loom/ui test` で全 PASS（既存 + Phaser mount/unmount lifecycle test 追加）。`tag m3.0-complete` 設置、`m0`〜`m2.1-complete` 全保持。

## M3.1: Plan View + Gantt + 双方向同期

- **REQ-033**: SPEC §3.6.9.2（PLAN.md 双方向同期 = hybrid debounce + last-write-wins + `plan_conflict_detected` toast）+ §3.6.9.3（Gantt = 自前 SVG）+ §3.6.9.4（toast 6 event 化）+ §3.6.9.7（Visual regression check = Playwright e2e、res-001 確定）に整合する形で実装。Plan View 短期 (TodoWrite mirror) が daemon `todoChange` subscription で live 更新。Plan View 長期 (plan_items) tree edit が GUI → daemon → DB 流通、500ms debounce で write-back。PLAN.md 外部 edit を chokidar 検知 → daemon → UI へ push、conflict 時 `plan_conflict_detected` toast push + localStorage backup（GUI 側 change を保持、user 手動 restore 可）。Gantt SVG が plan_items 進捗を `<rect>` bar + `<line>` grid + `<text>` label で描画、3 theme 切替で `var(--color-bar)` 即反映、bar click で Agent Detail navigate。Playwright e2e infra 導入（`@playwright/test` devDep、`ui/e2e/` dir、`pnpm --filter @claude-loom/ui e2e` script、CI workflow 並列 step）+ Room View pop theme screenshot baseline 1 件確立（`expect(page).toHaveScreenshot()` で green、test 大量化は M3.2 以降に分配）。`pnpm --filter @claude-loom/ui test` 全 PASS（debounce / conflict toast / Gantt SVG render test 追加、既存 vitest test の green 状態保持）、`pnpm --filter @claude-loom/ui e2e` 全 PASS、`pnpm --filter @claude-loom/daemon test` 全 PASS（chokidar + debounce + conflict resolution test 追加）。`tag m3.1-complete` 設置、`m0`〜`m3.0-complete` 全保持。

## M3.2: Session List + Agent Detail + notes

- **REQ-034**: Session List で filter (project/role) + sort (started_at) 動作、daemon `session` subscription で live 更新。Agent Detail で dispatch 履歴表示 + 注目フラグ書込（daemon `agent.markAttention` mutation）動作。notes 書込（daemon `note.create` mutation）で plan_items にひも付き、Agent Detail から添付可能。`pnpm --filter @claude-loom/ui test` 全 PASS、`pnpm --filter @claude-loom/daemon test` 全 PASS。`tag m3.2-complete` 設置、`m0`〜`m3.1-complete` 全保持、これで M3 系列 closure（Phase 1 MVP の core experience 完成）。

## M5: Integration + Polish (uninstall)

- **REQ-036**: `uninstall.sh --dry-run --yes` 実行では symlink は削除されない（変更なし）
- **REQ-037**: `install.sh` 実行後に `uninstall.sh --yes` を実行すると agents / commands / skills の全 loom-* symlink が削除される（install → uninstall round-trip）
- **REQ-038**: `uninstall.sh --yes` のデフォルト動作では `.claude-loom/` local state は保持される
- **REQ-039**: `uninstall.sh --yes --purge-state` 実行で `.claude-loom/` ディレクトリが削除される
- **REQ-040**: `uninstall.sh --yes` は exit code 0 で完了する

## M0.11.1: Lifecycle Tracking Architecture

- **REQ-035**: SPEC §3.9.11（Lifecycle Tracking Architecture）+ §6.9.6（pending.json schema v2、`applied_in` + `apply_history` field）+ §6.9.7（applied_summary.json schema、retro-pm Stage 0 lazy build 5 step）+ §6.9.4.5（learned_guidance auto-prune rule、`ttl_sessions` main + `last_used_in` audit hybrid）に整合する形で実装。`agents/loom-retro-pm.md` Stage 0 で過去全 retro session の pending.json scan + applied_findings 集約 → `<project>/.claude-loom/retro/<retro_id>/applied_summary.json` write 動作。4 lens（pj-judge / process-judge / meta-judge / researcher）の dispatch prompt prefix に `applied_summary_path: <path>` 注入、lens は `Read` tool で参照、stale check を Stage 1 内で自前実行。`agents/loom-retro-counter-arguer.md` の stale finding detection section（M3.0 retro proc-NEW-1 由来）を **物理削除**（SPEC §3.9.x P4 理想形「symptomatic patch 構造的解決後の rollback」初例として archive）。`agents/loom-retro-aggregator.md` に learned_guidance auto-prune logic（ttl_sessions auto-deactivate + last_used_in update）追加。既存 4 retro session（2026-04-29-001 / 2026-05-02-001 / 2026-05-02-002 + 古い 1 件確認）の pending.json に `applied_in` + `apply_history` field 後付け migration script 完了 + schema_version 1 → 2 migrate。`templates/{user,project}-prefs.json.template` に `last_used_in` field example 追加。`tests/{prefs,retro,agents}_test.sh` で `applied_in` schema + `applied_summary` build + auto-prune assertion + counter-arguer stale check section 不在 assertion 追加。dry-run test で applied_summary mechanism 動作確認（rollback 前安全網）。`./tests/run_tests.sh` で **13 PASS** 維持（既存 12 + 新規 `dry_run_applied_summary_test.sh` = 13）。`tag m0.11.1-complete` 設置、`m0`〜`m3.0-complete` 全保持。

## M5 t1: Frontend Design Handoff

- **REQ-041**: `docs/PIXEL_ART_HANDOFF.md` が存在 + 非空 + 4 section 以上含む。Vision section（SPEC §12 ビジュアル方向性）、placeholder state section（13 agent sprite identifier + tile map + 3 theme color token）、制作 option section（Option A/B/C/D）、post-MVP migration plan section を含む。`tests/docs_pixel_art_test.sh` でカバー。

## M5 t6: README + リリース準備

- **REQ-042**: `README.md` に `## ライセンス` section 存在（placeholder 許容）、`Phase 1 MVP` 完成記述あり、`M5` milestone 言及あり、`Phase 2 以降` section 存在。`CHANGELOG.md` に `[0.1.0]` entry 存在かつ `### Added` section 非空。root `package.json` / `daemon/package.json` / `ui/package.json` に `version` field（文字列型）存在。`tests/docs_release_test.sh` でカバー。

## M5 t2: End-to-end Verification

- **REQ-043**: `docs/M5_E2E_REPORT.md` が存在・非空・5 section（Executive summary / 7 verification / follow-up / Phase 2 / 結論）含む。`tests/m5_e2e_test.sh` が 7-stage aggregate harness として動作（route integrity 静的 check + install/uninstall round-trip check + deliverables check）。`bash tests/run_tests.sh` で **17 PASS** 達成（m5_e2e_test.sh 追加）。`pnpm --filter @claude-loom/daemon test` / `pnpm --filter @claude-loom/ui test` / `pnpm --filter @claude-loom/ui e2e` / `pnpm --filter @claude-loom/daemon build` + `pnpm --filter @claude-loom/ui build` が全て pass（UI build blockers — NOTE_ATTACHED_TYPE browser-safe 化 + process.env → import.meta.env 修正を含む）。Phase 1 MVP closure 認定。`tests/m5_e2e_test.sh` でカバー。

## M0.11.3: UI Smoke Test Skill

- **REQ-044**: SPEC §3.6.11（UI Smoke Test Skill）+ §10.4（Browser-interactive verification layer）に整合する形で `skills/loom-ui-smoke/` 新設。`SKILL.md` が valid frontmatter（name = `loom-ui-smoke` / description 必須）+ Stage 1 戦略 derive prompt + Stage 2 Playwright MCP `browser_*` tool 駆動 instruction（deterministic order）+ Stage 3 report 生成手順を含む。`scripts/format-report.sh` が deterministic report formatter として動作（`docs/smoke-tests/<date>-<scope>/report.md` + `findings.json` 生成、`templates/findings.schema.json` で JSON schema validate）。`scripts/start-servers.sh` が hybrid Option C 補助（port 5757/5173 listen 確認、`--auto-start` flag 対応）。`commands/loom-ui-smoke.md` slash command が valid frontmatter で `--scope=full|route:<name>|smoke-only` parameter 認識。`agents/loom-developer.md` + `agents/loom-pm.md` に suggest skill 参照記述（§3.6.11.10 Consumer agents）+ milestone closure 自律 invoke logic（loom-pm のみ）追記。skill self-test: main HEAD で `/loom-ui-smoke` 実行、hotfix 完了後の 4 bug fix 状態を browser smoke verify、`docs/smoke-tests/<date>-<scope>/` に成果物生成 + `findings.json` JSON schema validate pass。`bash tests/run_tests.sh` で **18 PASS** 達成（skills_test.sh + commands_test.sh 拡張で loom-ui-smoke カバー）。`tag m0.11.3-complete` 設置、`m0`〜`m5-complete` 全保持。

## M0.11.4: Design Implementation Pass (aesthetic MVP completion)

- **REQ-045**: SPEC §3.6.12（Design Implementation）+ §3.6.9.1 改訂 (Phaser α-1 → DOM/SVG α-2 strategy A) に整合する形で `claude-room-handoff.zip` design bundle を full port。`ui/src/components/CatSprite.tsx` (16x16 pixel grid SVG、9 hat × 3 pose、`shapeRendering="crispEdges"` + `image-rendering: pixelated`) + `ui/src/data/roster.ts` (13 agent metadata: id / role / jp / name / breed / quote / hat / fur / cheek / group)。`ui/src/styles/tokens.css` に 3 theme palette (`:root` + `.theme-dusk` + `.theme-night`、各 25+ `--p-*` variable) + RPG primitives (`.rpg-frame` / `.rpg-frame-tight` / `.rpg-title` / `.rpg-label` / `.dot` / `.chip` / `.exp-bar` / `.btn-px` 5 variant) + Room primitives (`.room-*` / `.room-poster-*` / `.room-island-*` / `.room-modal-*` / `.subroom-*`)。`ui/src/views/room/` 全面書直し (RoomBackground / DeskStation / wall-posters Gantt+Plan+Consistency / Islands / SubroomClone / AgentDetailPanel / RetroGathering / RoomView orchestration)。15 view port (PlanView / GanttView / ConsistencyView / RetroView / CharSheet / ThemeShowcase / WorktreeView / SubroomView / CustomizationView / LearnedGuidanceView + 既存 view 全面書直し)。新 view (SessionListView / TokenMeterView / ProjectSettingsView) を RPG 言語で 新規設計、Sidebar + DisciplineHeader RPG style update。`ui/package.json` から `phaser` dependency 削除 + `ui/src/views/room/{PhaserCanvas.tsx,scenes/RoomScene.ts,agentSpriteSync.ts}` 物理削除。Playwright e2e baseline 再生成 (3 theme: room-pop.png + room-dusk.png + room-night.png)。`loom-ui-smoke` skill full scope execute、`docs/smoke-tests/<date>-m0.11.4-aesthetic-verify/` に成果物生成、design vision 達成確認。`bash tests/run_tests.sh` で 18 PASS 維持、`pnpm --filter @claude-loom/ui test` 全 PASS、`pnpm --filter @claude-loom/ui e2e` 全 PASS。`tag m0.11.4-complete` 設置 (aesthetic MVP completion marker)、`README.md` 「Phase 1 MVP completed」記述を完全達成 marker に update、`m0`〜`m0.11.3-complete` 全保持。

## M0.11.5 hotfix: install.sh daemon symlink bootstrap

- **REQ-046**: `install.sh` 実行で `~/.claude-loom/daemon.js` symlink が `<project>/daemon/dist/index.js` を指す形で配置される。SPEC §3.2 lazy daemon auto-launch flow が機能するための前提整備（M0.11.5 retro 2026-05-06-001 F-USER-001 critical hotfix、loom-launch-ui.sh が `DAEMON_BIN=$HOME/.claude-loom/daemon.js` を参照しとるが install.sh に bootstrap step 不在）。`tests/install_test.sh` の REQ-046 assertion でカバー。`daemon/dist/index.js` が存在しない場合は warning log + skip（idempotent）、build 後の再 install で symlink 設置完了。

## M0.11.7: PM Auto-Go Entry

- **REQ-047**: `agents/loom-pm.md` に Spec Phase Completion Hook（PM Auto-Go Entry）が追加され、SPEC §3.6.8.10 を SSoT として参照し、3 軸 AND 条件 / 3 信頼レベル分岐 / `/loom-go` override 動作 / impl keyword list（spec keyword list と分離）/ 既存 M0.11.6 Session Start Hook 構造の維持を含む。`tests/m0117_t3_loom_pm_auto_go_test.sh` でカバー。

- **REQ-048**: `agents/loom-pm.md` の Spec Phase Completion Hook section 内 2 placeholder（impl intent keyword list / 確認 prompt template）が実内容で埋まっていること。impl 系 keyword list が 10 個以上（着手・kick off 等含む）かつ spec 系 keyword list（M0.11.6）と分離維持。高信頼 template に `/loom-spec` + `/loom-status` bypass option 明示。中信頼 3 択 template（impl 開始 / spec 修正 / status 確認）が記述済み。`tests/m0117_t4_t5_placeholders_test.sh` でカバー。

## M0.X-runtime-mode-recovery t4: /mode probe + Vite redirect in loom-launch-ui.sh

- **REQ-050**: `hooks/loom-launch-ui.sh`
<!-- NOTE: REQ-050 is used by t4 task above -->

## M0.X-runtime-mode-recovery t8: post-install stale tsx watch detection

- **REQ-051**: `install.sh` post-install check で stale tsx watch プロセス（`tsx.*server.ts` pattern）を `pgrep -f` で検出、存在時 WARNING + PID リスト + cleanup suggestion 出力（zombie 自動 kill なし、user 明示提案のみ）。pgrep 不在 / エラー時は `|| true` で graceful fallback し install.sh exit 0 を維持。`tests/install_post_check_test.sh` でカバー（zombie あり / なし / pgrep unavailable 3 scenario）。 の warm-start path (health-check 通過後) が `/mode` endpoint を probe し mode に応じて分岐する (SPEC §3.2.2 dev daemon 検出時の lazy launch 挙動 4 step 準拠)。`mode=dev` かつ Vite (`LOOM_VITE_URL` override 対応、default `:5173`) 応答あり → Vite URL を browser open + stdout。`mode=dev` かつ Vite 応答なし → warning log (stderr) + Vite URL stdout のみ (browser open skip)。`mode=prod` warm → 既存挙動 (browser open skip)。`/mode` endpoint fail / unparseable → 既存挙動 fallback (browser open skip)。`tests/loom_launch_ui_mode_probe_test.sh` でカバー。

## M0.X-runtime-mode-recovery t2/t3: /mode endpoint + LOOM_DEV_MODE switch

- **REQ-049**: `daemon/src/server.ts` の static serving 判定が `LOOM_DEV_MODE` env var ベース（`isDevMode = !!process.env.LOOM_DEV_MODE`、`NODE_ENV` 依存を deprecate）に切替済みかつ `GET /mode` endpoint が SPEC §3.2.1 shape（`mode` / `entry` / `version` / `started_at` / `pid` / `ui_serving` 6 fields）を返す。`LOOM_DEV_MODE` truthy → `mode=dev` + `ui_serving=false`、unset + ui/dist 存在 → `mode=prod` + `ui_serving=true`、unset + ui/dist 不在 → `mode=prod` + `ui_serving=false`。`LOOM_ENTRY` 各値（`lazy-launch` / `pnpm-dev` / `manual`）が `/mode` response の `entry` field に反映。`pnpm --filter @claude-loom/daemon test test/server-mode-endpoint.test.ts` 13 PASS、`test/server-static.test.ts` 11 PASS（`NODE_ENV` mutation → `LOOM_DEV_MODE` mutation 移行済み）。

## M0.X-runtime-mode-recovery t7: /loom-stop --all zombie cleanup

- **REQ-052**: `hooks/loom-stop.sh` が `--all` flag に対応する。引数なしは既存挙動（graceful shutdown 1 daemon、fail-silent）を維持。`--all` では (1) POST /shutdown 試行、(2) daemon.pid stale kill、(3) `pgrep -f "tsx.*server\.ts"` zombie kill、(4) `pgrep -f "node.*\.claude-loom/daemon\.js"` manual launch kill、(5) killed/not-found report を stdout 出力、(6) best-effort exit 0。`commands/loom-stop.md` が `--all` flag description を含む。テスト専用 `LOOM_TEST_ZOMBIE_PIDS` env var で mock PID 直接指定可能。`tests/loom_stop_all_test.sh` でカバー。

## M0.X-runtime-mode-recovery t5/t6: pnpm dev preflight + LOOM_DEV_MODE auto-inject

- **REQ-053**: `daemon/package.json` の `dev` script が `LOOM_DEV_MODE=1 LOOM_ENTRY=pnpm-dev` を auto-inject + `bash scripts/pnpm-dev-preflight.sh` を tsx watch 起動前に invoke。`daemon/scripts/pnpm-dev-preflight.sh` が SPEC §3.2.2 prod daemon 検出時挙動を実装: `/health` + `/mode` probe で `mode=prod` 検出 → ERROR + PID + entry を stderr に出力して exit 1 (tsx watch 起動ブロック)、dev daemon / daemon 不在 / probe fail は exit 0 (graceful)。`tests/m0x_t5_t6_preflight_test.sh` でカバー (13 scenario)。

## M0.X-runtime-mode-recovery t11: daemon runtime mode E2E smoke test

- **REQ-054**: `tests/daemon_runtime_mode_test.sh` が 3 boot scenario の integrated E2E smoke test を提供する。(1) prod mode (LOOM_ENTRY=lazy-launch): `/mode` → `mode=prod` + `entry=lazy-launch` + `ui_serving=true` (ui/dist 存在時)、`/` → 200 + HTML。(2) dev mode (LOOM_DEV_MODE=1 LOOM_ENTRY=pnpm-dev): `/mode` → `mode=dev` + `entry=pnpm-dev` + `ui_serving=false`、`/` → 404 (static skip 確認)。(3) manual mode (env なし): `/mode` → `mode=prod` + `entry=manual` + SPEC §3.2.1 規定 6 fields 全存在確認。test port は 15870–15872 (既存 test 15850–15863 と衝突なし)、`LOOM_PORT` env var で daemon が custom port を受け付ける (CLI entry の default 5757 を override)。`tests/daemon_runtime_mode_test.sh` でカバー (15 assertion)。

## M0.X-runtime-mode-recovery hotfix: start_daemon port bind verify (Bug A)

- **REQ-055**: `hooks/loom-launch-ui.sh` の `start_daemon()` が `nohup` 後に `/health` polling (default 5 attempts × 0.5s) で実際の port bind 成功を verify する。verify 失敗時は exit 1 を返し `main()` が `open_browser` を skip（Bug A 修正: nohup success ≠ port bind success）。env override `LOOM_DAEMON_BOOT_MAX_ATTEMPTS` で polling 試行数を制御可能（test fixture 用）。`while [ "$i" -le ... ]` POSIX 互換ループ使用（bash 3.x 対応）。happy path (daemon が /health で応答) では依然 exit 0 を返し open_browser が呼ばれる（regression 防止）。`tests/loom_launch_ui_bug_a_test.sh` でカバー（Test A: dead daemon → no browser / Test B: healthy daemon → browser called / Test C: attempt override effective）。既存 `tests/loom_launch_ui_test.sh` test 7 fixture が fake_node でバックグラウンド HTTP server を起動する形に更新（Bug A fix との整合）。SSoT: SPEC §3.2.3 (Boot health-check polling) — retro 2026-05-06-003 F-pj-002 で codify。

## M0.X-hook-ingest-recovery t3/t4: hook payload cross-platform timestamp fix

- **REQ-056**: 5 種 bash hook script (`hooks/{pre_tool,post_tool,session_start,stop,SubagentStop}.sh`) が生成する JSON payload の `payload.timestamp` フィールドが macOS / Linux 両環境で pure integer (数字のみ) であること。各 hook script が `ts_ms()` cross-platform helper 関数を持ち、`date +%s%3N` の直接使用を行わないこと。`ts_ms()` は python3 → node → `date +%s` ×1000 の 3 段 fallback で ms 精度を取得。`tests/hook_ingest_integration_test.sh` でカバー（Test 1: `date +%s%3N` integer check / Test 2: PAYLOAD JSON validity per hook / Test 3: ts_ms() helper presence + no-raw-date assert / Test 4: daemon live POST ok:true、daemon 不在時 skip）。**rationale**: macOS BSD `date` は `+%3N` を非サポートで生文字 `N` を残置、`TS=17780770293N` 生成 → JSON parse 失敗 → daemon 400 spam が M0.X-hook-ingest-recovery root cause。同 class の cross-platform format mismatch を構造的に detect する。

## M0.X-hook-ingest-recovery post-tag t6: command-frequency.log silent failure fix

- **REQ-057**: `hooks/post_tool.sh` が Claude Code SDK の stdin JSON input から `tool_name` + `tool_input.command` を抽出、SlashCommand event 時に `~/.claude-loom/command-frequency.log` (env override `LOOM_FREQUENCY_LOG` で path 変更可、`LOOM_NO_FREQUENCY_LOG=1` で opt-out) に append する。stdin 不在時 (test invoke 等) は `CLAUDE_TOOL_*` env var fallback。`tests/command_frequency_log_test.sh` でカバー (4 scenario: stdin SlashCommand / opt-out / env var fallback / non-SlashCommand)。**rationale**: SDK は env var ちゃう stdin JSON で hook input 渡す仕様、env var only path は永久に false negative で silent failure (retro 2026-05-06-003 F-meta-004 由来、post-tag-hotfix protocol §3.6.8.11 適用第 1 例)。

## M0.X-hook-ingest-recovery post-tag t5: parallel batch SessionStart interaction test

- **REQ-058**: `tests/hook_parallel_batch_interaction_test.sh` が parallel SessionStart 多重発火 + daemon load 観察 + Bug A trigger interaction を structural verify する (4 scenario: 単発 baseline / 3 並列 200 応答 / 並列中 /health responsive / daemon 不在 fail-silent)。daemon 不在時は Test 2-3 skip + WARN (CI environment friendly)。**rationale**: retro 2026-05-06-003 F-proc-002 由来、parallel batch dispatch + SessionStart 多重発火 interaction の structural test gap を埋める。post-tag-hotfix protocol §3.6.8.11 適用第 1 例。

## feat/dev-mode-ux item 1A: root pnpm dev で daemon + UI 並走

- **REQ-059**: root `package.json` の `scripts.dev` が `concurrently` 経由で `@claude-loom/daemon` + `@claude-loom/ui` を並走起動すること、`devDependencies` に `concurrently` を宣言していること。`tests/root_dev_script_test.sh` でカバー (4 assertion: scripts.dev に concurrently / @claude-loom/daemon / @claude-loom/ui の 3 token 同時包含 + devDependencies.concurrently 存在)。**rationale**: `pnpm --filter @claude-loom/ui dev` 単独運用は WS が daemon (:5757) に繋がらず `daemon_disconnected` toast が exponential backoff 周期 (1s/3s/7s/15s/31s/61s/91s...) で連発するため、root 1 コマンドで両方起動する workflow を契約として固定する。escape hatch (`dev:daemon` / `dev:ui`) は YAGNI で省略 — `pnpm --filter @claude-loom/<x> dev` 直接叩けば等価。

## feat/dev-mode-ux item 1B: state-style toast の dedup

- **REQ-060**: `ui/src/notifications/toastBus.ts` の `emitDaemonDisconnected` / `emitDaemonReconnected` が **stable id** (`'daemon_disconnected'` / `'daemon_reconnected'`) を発行する。`ui/src/notifications/ToastContainer.tsx` が同じ id の toast を受信した時、append ではなく **既存 entry を置換** (古い timer は clear、新 message + 新 timer を反映) する。occurrence-style emitter (`emitConsistencyFindingNew` / `emitSubagentFailed` / `emitProjectAdded` / `emitPlanConflictDetected` / `emitSpecChangeDetected`) は per-call `Date.now()` 付き id を維持し、複数 stack を許容する。`ui/test/toast.test.tsx` でカバー (5 assertion 追加: emitDaemonDisconnected stable id / emitDaemonReconnected stable id / emitConsistencyFindingNew unique id / Container dedup by id / Container 複数 stack 維持)。**rationale**: WS exponential backoff (1s/3s/7s/15s/...) で `handleClose` + `handleError` が `emitDaemonDisconnected` を毎回呼ぶ。従来は `id: 'daemon_disconnected-${Date.now()}'` で全 emit が unique id → toast が piling up。state を表す toast (daemon connection) は singleton、occurrence を表す toast (findings 等) は countable、という UX 区別を id 設計に反映。

## feat/install-auto-build item 2: install.sh が daemon + UI を auto-build

- **REQ-061**: `install.sh` が symlink 設置の後、daemon.js symlink 設置の前に **auto-build block** を実行する。`LOOM_NO_BUILD=1` で skip (codebase 既存の `LOOM_NO_*` 命名規約に整合)、`pnpm` が PATH 不在時は WARN + skip + 完走、pnpm 利用可能時は (1) `node_modules/` 不在なら `pnpm install`、(2) `pnpm --filter @claude-loom/daemon build`、(3) `pnpm --filter @claude-loom/ui build` を順に実行。各 build 失敗は WARN 出力のみで install.sh の最終 exit を妨げない (graceful)。build idempotency は tsc/vite 内部の incremental cache に委ねる (install.sh 側で dist 存在 check しない、YAGNI)。`tests/install_auto_build_test.sh` で 7 assertion カバー (LOOM_NO_BUILD=1 で fake pnpm 一切呼ばず + skip メッセージ + install 完走 / fake pnpm log に daemon+ui filter build 両方 invocation / pnpm PATH 不在で WARN + 完走)。**rationale**: end-user 体験 — 旧 README は `git clone → ./install.sh → pnpm install` 順、build は user 任せで daemon/dist or ui/dist の片方が無いと daemon symlink skip → `/loom` invoke で silent failure に陥る。「`./install.sh` 1 発で動く」UX を契約として固定する。dev contributors は `LOOM_NO_BUILD=1` で escape 可能 (CI / 手動 build 派 / install.sh test 自身が利用)。

## M0.15 Phase 1: WebSocket reducer foundation

- **REQ-062**: `redesign/api/websocket.ts` の `LiveScenarioStore` が 8 event reducer method (`applyTodoChange` / `applyPlanChange` / `applyWorktreeChange` / `applyLearnedGuidanceChange` / `applyDisciplineMetricUpdate` / `applyApprovalRequest` / `applySessionChange` / `applyFindingNew`) を持ち、各 method が immutable snapshot update + `this.emit()` を行う。WS `addEventListener("message")` 内が switch 分岐に拡張され、9 event type (既存 `agent.change` + 新 8 type) を routing する。`redesign/api/types.ts` に 8 payload 型 (`TodoChangePayload` / `PlanChangePayload` / `WorktreeChangePayload` / `LearnedGuidanceChangePayload` / `DisciplineMetricUpdatePayload` / `ApprovalRequestPayload` / `SessionChangePayload` / `FindingNewPayload`) を export。`LiveScenarioStore` が test-only export として公開され、`ui/test/redesign/websocket-reducer.test.ts` の 11 unit test (regression 1 + 新 8 reducers) が全 pass。**rationale**: Phase 2 の 9 画面 redesign port が `useScenario()` 経由で event-driven な aggregated state を消費する前提条件として、reducer foundation を先に確立する。

## M0.15 Phase 2 batch A: Gantt/Plan/Worktree redesign port

- **REQ-063**: `ui/src/views/gantt/GanttView.tsx` が `useScenario().gantt` (redesign WS hook) を data source とし、`GanttData.rows` を `worktree` キーでグループ化して worktree-collapsible レイアウトを描画する。各 `GanttRow.bars[].kind` に応じた CSS color (`busy=var(--p-success)` / `review=var(--p-accent)` / `tdd=var(--p-warn)` / `fail=var(--p-error)`) を適用し `data-kind` attribute でテスト可能にする。`live: true` の row のみ `data-testid="gantt-live-cat"` 付きの walking cat sprite を bar track に描画する。now-line は `data-testid="gantt-now-line"` で識別可能。`ui/test/views/gantt/gantt-mock-active.test.tsx` の 4 smoke test (row labels × 6 / data-kind × 4 kinds / now-line / live cat × 2) が全 pass。**rationale**: M0.15 Phase 2 batch A t2 — 既存の useGanttData ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Gantt を live-driven に置換する。worktree グループ化と walking cat live 演出は redesign/screens/gantt.jsx 視覚 SSoT に準拠。

- **REQ-064**: `ui/src/views/plan/PlanView.tsx` が `useScenario()` (redesign/api/websocket) のみに依存し、usePlanItems / useTodoWrite / usePlanMutations を import しない。左ペイン (data-testid="plan-long-term") が scenario.milestones をマイルストーンカード (`plan-milestone` testid + `milestone-progress-bar` + `milestone-progress-fill` + `milestone-child`) で表示し、右ペイン (data-testid="plan-short-term") が scenario.todos を todo-item (data-status 属性付き) + scenario.todosUpdatedAt (`todos-updated-at` testid) で表示する。タブ (active / 完了 archive / 編集) の切替で左ペイン表示が切替わる。`ui/test/views/plan/plan-mock-active.test.tsx` の 4 ケース全 pass: todos 4件 status glyph / milestones 2件 progress bar / milestone children 3件 nested / todosUpdatedAt timestamp。`pnpm --filter @claude-loom/ui test` 全 pass (regression なし)。**rationale**: M0.15 Phase 2 batch A t3 — 既存の usePlanItems / useTodoWrite ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Plan view を read-only display に置換する。write hookup は Phase 5 t16 で別 task。

## M0.15 Phase 2 batch B: Customization redesign port

- **REQ-066**: `ui/src/views/customization/CustomizationView.tsx` が `useScenario().customization` (redesign/api/websocket) を data source とし、MOCK_SETTINGS ハードコード fixture を撤去。各 agent の effective model + preset を ROSTER 順で 13 行テーブル表示する。各行は `agent-row` testid + `data-agent-id` 属性 + `model-selector` testid (opus/sonnet/haiku ボタン、effective model を `aria-pressed=true`) + 4 preset button (`preset-btn-{id}` testid、emoji + name 表示) + `chain-expand-btn` testid (chain.length 表示、chain.length > 1 で warn color)。右パネルに `chain-detail-panel` testid (SCOPE CHAIN 説明)、chain expand クリックで chain 各 layer の `chain-scope-tag` (DEFAULT/USER/PROJECT) を展開表示。`ui/test/views/customization/customization-mock-active.test.tsx` の 7 ケース全 pass + 既存 `ui/test/views/customization.test.tsx` (14 tests) regression なし。**rationale**: M0.15 Phase 2 batch B t5 — 既存の MOCK_SETTINGS hardcoded fixture を撤去し、redesign bundle の useScenario 経由データで Customization view を scope chain trace 付きに置換する。write hookup (PUT /customization/:id) は Phase 5 t16 で別 task。redesign/screens/customization.jsx の table + chain detail panel 視覚 SSoT に準拠。

## M0.15 Phase 2 batch B: Guidance redesign port

- **REQ-065**: `ui/src/views/guidance/GuidanceView.tsx` が `useScenario().guidance` (redesign/api/websocket) を data source とし、`GuidanceItem[]` を client-side filter (activeOnly / agentFilter / searchQuery) でフィルタリングして表示する。各エントリが `guidance-item` testid + `guidance-source` (from フィールド) + `guidance-scope` chip + `guidance-category` badge + `guidance-toggle` (retire noop button、active entries のみ) を持つ。`diff` フィールドを持つエントリは `guidance-diff-toggle` ボタンを表示し、クリックで `guidance-diff-panel` (before/after diff) を展開する。filter bar は `filter-active-only` checkbox (default: checked) + agent select + text search を持ち、activeOnly=true で inactive entries を非表示、uncheck で全 entries 表示。`ui/test/views/guidance/guidance-mock-active.test.tsx` の 5 ケース全 pass: 全5件表示 / toggle+scope+category per entry / diff expandable / source links × 5 / filter narrows entries。既存 `ui/test/views/guidance.test.tsx` (13 tests、`LearnedGuidanceView` 後方互換) も regression なし。**rationale**: M0.15 Phase 2 batch B t6 — 既存の MOCK_GUIDANCE ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Guidance view を read-only display に置換。write hookup (DELETE /guidance/:id retire / toggle active) は Phase 5 t16 で別 task。redesign/screens/guidance.jsx の filter bar + diff expandable + source link 視覚 SSoT に準拠。

## M0.15 Phase 2 batch B: AgentDetailPanel redesign port

- **REQ-067**: `ui/src/views/room/AgentDetailPanel.tsx` が `useScenario()` (redesign/api/websocket) を data source とし、ハードコード fixture (DEFAULT_HISTORY / 固定 token 値 / 固定 currentTask) を撤去。drawer overlay レイアウト (redesign/screens/agent-detail.jsx 視覚 SSoT) で、agent state は `scenario.agents[agent.id]`、stream tail は `scenario.stream.filter(s.who===agent.name)`、token usage は `scenario.tokens.byAgent.find(t.agentId===agent.id)` を source とする。`agent-detail-status` / `agent-detail-current-tool` / `agent-detail-current-reasoning` / `agent-detail-tokens` testid を持つ。既存 M3.2 t2 backward compat (agent-attention-toggle / agent-dispatch-history / onAttentionToggle / dispatchHistory props) および M0.11.4 t10 backward compat (rpg-frame class / jp field / CatSprite size>=64 / quote) を維持。`ui/test/views/room/agent-detail-mock-active.test.tsx` の 6 ケース全 pass: agent identity / status / currentTool+reasoning / stream filter / token usage / onClose。`pnpm --filter @claude-loom/ui test` 全 pass (910 tests、regression なし)。**rationale**: M0.15 Phase 2 batch B t7 — RoomView から猫クリックで表示される agent detail drawer を useScenario 駆動に置換。既存 test 後方互換を完全維持しつつ live data を wire in。

## M0.15 Phase 2 batch C: Settings redesign port

- **REQ-068**: `ui/src/views/project-settings/ProjectSettingsView.tsx` が `useScenario().settings` (redesign/api/websocket) を data source とし、useProjectSettings (旧 tRPC hook) を撤去。redesign/screens/settings.jsx 視覚 SSoT に基づく 8 フィールド form (daemonPort / worktreeBase / retroSchedule / consistencyScope / hooks / defaultReviewers / parallelLimit / logRetention) を `Row` layout component で表示する。各フィールドに対応する data-testid (`setting-daemonPort` / `setting-worktreeBase` / `setting-retroSchedule-cron` / `setting-retroSchedule-label` / `setting-consistencyScope` / `setting-hook-{key}` × 3 / `setting-reviewer-{id}` × 3 aria-pressed / `setting-parallelLimit` / `setting-logRetention-days`) を持つ。保存・取消ボタンは controlled draft state で dirty 検知 (保存 noop = Phase 5 t16 deferral)。`ui/test/views/project-settings/settings-mock-active.test.tsx` の 6 ケース全 pass (8 field を fully covering) + 既存 `ui/test/views/project-settings.test.tsx` (17 tests、useScenario mock に更新済) regression なし。`pnpm --filter @claude-loom/ui test` 全 pass (913 tests)。**rationale**: M0.15 Phase 2 batch C t10 — M0.11.4 ハードコード fixture (useProjectSettings hook) を撤去し、redesign bundle の useScenario 経由データで Settings view を read-only display に置換。write hookup (PUT /settings) は Phase 5 t16 で別 task。

## M0.15 Phase 2 batch C: Tokens redesign port

- **REQ-069**: `ui/src/views/tokens/TokensView.tsx` が `useScenario().tokens` + `useScenario().pricing` (redesign/api/websocket) を data source とし、旧 `TokenMeterView` (useTokenUsage ハードコード fixture) を撤去して新しい redesign 駆動実装に置換する。7-day daily cost bar chart (`daily-bar-{day}` testid + `data-cache-ratio` 属性) + per-agent table (`agent-row-{agentId}` testid + `agent-cost-{agentId}` testid) + top 4 stat cards (TOTAL COST / INPUT TOK / OUTPUT TOK / CACHE HIT) + efficiency hints section (`efficiency-hints` testid、rule-based: cache-hit < 0.7 の日 + opus output 少エージェント) を描画する。pricing × tokens から USD cost を client-side 計算 (costOf helper)。`ui/test/views/tokens/tokens-mock-active.test.tsx` の 5 ケース全 pass: 7-day daily bar chart / per-agent table with model + tokens / cache-hit ratio per day / cost USD computed from pricing × tokens / efficiency hint when cache-hit < 0.7 (Sat 0.62)。既存 `ui/test/views/tokens.test.tsx` (17 tests、TokenMeterView 後方互換) + 全 test suite 913 tests regression なし。**rationale**: M0.15 Phase 2 batch C t9 — 既存の useTokenUsage ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Tokens view を usage + cost analytics dashboard に置換する。redesign/screens/tokens.jsx の visual SSoT に準拠。

## M0.15 Phase 2 batch C: Sessions redesign port

- **REQ-070**: `ui/src/views/session-list/SessionListView.tsx` が `useScenario().sessions` (redesign/api/websocket) を data source とし、useSessionList (tRPC) 依存を撤去。左ペイン (data-testid="session-list") が split-panel レイアウト (左 = list + filter bar、右 = detail panel) で表示する。各エントリが `session-entry` testid + `session-verdict-badge` (PASS/FAIL 背景色) を持つ。filter bar は `session-search-input` (summary/file search) + `session-filter-agent` (agentRoot filter) + `session-filter-verdict` (PASS/FAIL/all) を持ち、client-side でエントリを絞り込む。右パネルは `session-detail-panel` testid、未選択時は説明プレースホルダー、エントリ選択時は FILES TOUCHED + RELATED (relatedRetro / relatedFindings) + TRANSCRIPT REPLAY stub を表示。`ui/test/views/session-list/sessions-mock-active.test.tsx` の 5 ケース全 pass: 全 7 entries / verdict badges (PASS×5 + FAIL×2) / filesTouched+relatedFindings+relatedRetro / search+filter bar / search interaction。`ui/test/views/session-list.test.tsx` 14 ケース全 pass (redesign 駆動で再記述)。`pnpm --filter @claude-loom/ui test` 全 pass (regression なし)。**rationale**: M0.15 Phase 2 batch C t8 — 既存の useSessionList ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Sessions archive view を read-only display に置換。redesign/screens/sessions.jsx の split-panel + search + filter + detail panel 視覚 SSoT に準拠。

## M0.15 Phase 3 batch D: Retro redesign port

- **REQ-072**: `ui/src/views/retro/RetroView.tsx` が `useScenario().retroSession` (redesign/api/websocket) を data source とし、M0.11.4 ハードコード fixture (MOCK_SESSION / MOCK_LENSES / MOCK_FINDINGS) を撤去。redesign/screens/retro.jsx 視覚 SSoT に基づく 2-column レイアウト (左: lens summary 4 cards + transcript player scrubber、右: findings list + action plan tally) を描画する。lens cards は `lens-card-{id}` testid + `data-is-user` attribute (isUser lens highlight)。transcript entries は `transcript-entry` testid + `transcript-kind-{kind}` testid (intro/report/finding/rebuttal/verdict)。findings は `finding-item` testid + `finding-{id}` testid + `finding-sev` / `finding-category` badge testid。action plan は `action-plan` testid + `action-plan-{immediate|milestone|deferred}` testid。verdict badge は `retro-verdict-badge` testid。action buttons (accept/reject/defer/discuss) は noop (Phase 5 t16 write hookup 予定)。`ui/test/views/retro/retro-mock-active.test.tsx` の 6 ケース全 pass + `ui/test/views/retro.test.tsx` (13 tests、redesign 駆動で再記述) regression なし。`pnpm --filter @claude-loom/ui test` 全 pass (915 tests)。**rationale**: M0.15 Phase 3 batch D t12 — 既存の MOCK_* hardcoded fixture を撤去し、redesign bundle の useScenario 経由データで Retro view を transcript player + findings + action plan tally に置換する。redesign/screens/retro.jsx の visual SSoT に準拠。retro = マルチターン AI セッションの再生機という役割定義を timeline display で表現。

## M0.15 Phase 3 batch D: Consistency redesign port

- **REQ-073**: `ui/src/views/consistency/ConsistencyView.tsx` が `useScenario().findings` + `useScenario().consistencyState` (redesign/api/websocket) を data source とし、MOCK_FINDINGS ハードコード fixture を撤去。3 state (empty / running / has-findings) を `consistencyState` 値に応じて切替表示する。empty = `consistency-empty` testid、running = `consistency-running` testid、has-findings = 各 finding に `finding-card` testid + `data-sev` 属性 (高/中/低) + `status-badge-{id}` testid (open 以外のみ) + `expand-btn-{id}` (detail/suggest 展開) + 4 action button (`data-action=ack|fix|dismiss|discuss`、open finding のみ、noop)。`ui/test/views/consistency/consistency-mock-active.test.tsx` の 6 ケース全 pass (5 findings 表示 + sev badge × 3 種 + status badge × 3 + action button × 4 種 + expand click + empty state 切替)。既存 `ui/test/views/consistency.test.tsx` (17 tests、useScenario mock に更新) regression なし。`pnpm --filter @claude-loom/ui test` 全 pass (916 tests)。**rationale**: M0.15 Phase 3 batch D t11 — 既存の MOCK_FINDINGS ハードコード fixture を撤去し、redesign bundle の useScenario 経由データで Consistency view を read-only display に置換する。write hookup (POST /findings/:id/{ack,fix,dismiss}) は Phase 5 t16 で別 task。redesign/screens/consistency.jsx の 3 state 表示 + severity badge + action button 視覚 SSoT に準拠。

## M0.15 Phase 3 t13: PMChat implementation

- **REQ-074**: PM chat 機能が daemon + frontend 両方で実装される。daemon 側は 3 新 event schema (pm.message / pm.permission_request / pm.permission_resolved) が `daemon/src/events/types.ts` の loomEventSchema discriminated union に追加される。broadcaster に 3 emit 関数追加。`daemon/src/routes/pm.ts` に 4 REST endpoint stub (POST /pm/start、POST /pm/say、POST /pm/permission/:reqId、POST /pm/stop) が実装される。frontend 側は `redesign/api/websocket.ts` に applyPmMessage / applyPmPermissionRequest / applyPmPermissionResolved の 3 reducer 追加 (WS switch 3 case 追加)。`ui/src/views/pm-chat/PMChatPanel.tsx` が右カラム 340px の Slack-style chat panel (CHAT tab + STREAM tab)。`ui/src/views/pm-chat/PMApprovalToast.tsx` が med/low risk toast。`ui/src/views/pm-chat/PMApprovalModal.tsx` が high risk 中央 modal (誤クリック防止)。`ui/src/routing/AppShell.tsx` に右カラム mount slot 追加。`daemon/test/events/pm-events.test.ts` (pm schema + broadcaster emit × 3 event × 複数 test) + `daemon/test/routes/pm.test.ts` (4 endpoint test + router shape test) 全 pass。`ui/test/redesign/websocket-reducer.test.ts` に 3 reducer test 追加 (12 合計)。`ui/test/views/pm-chat/pm-chat-mock-active.test.tsx` + `ui/test/views/pm-chat/pm-approval-flow.test.tsx` 全 pass。`pnpm --filter @claude-loom/daemon test` 全 pass (546 tests)。`pnpm --filter @claude-loom/ui test` 全 pass (955 tests)。**rationale**: M0.15 Phase 3 t13 — PM chat 右カラム Slack-style panel + daemon pm.* sub-router stub + 3 WS event type + risk-based approval routing (high → modal / med/low → toast) の実装。実際の claude CLI spawn (stdin pipe) は Phase 5 t17 scope。

## M0.15 Phase 4 t15: Room posters scenario-driven

## M0.15 Phase 5 t16: Write API hookup (6 screens)

- **REQ-077**: M0.15 Phase 5 t16 — 6 redesign-port views (Customization / Guidance / Worktree / Consistency / Plan / ProjectSettings) の noop ボタンが実際の daemon 変更を起動する tRPC mutation hook に接続される。新規 hook: `useCustomizationMutation` (prefs.user.set / prefs.project.set)、`useGuidanceMutations` (learnedGuidance.toggle / learnedGuidance.delete)、`useWorktreeMutations` (worktree.create / worktree.remove / worktree.lock / worktree.unlock)、`useProjectSettingsMutation` (prefs.project.set)。既存 hook: `usePlanMutations` (upsertItem 接続)、`useConsistencyMutations` (acknowledgeFinding / markFindingFixed / dismissFinding / openInEditor)。各 view に `vi.mock` で mutation hook を stub 化した write テスト (6 新規テストファイル `*-write.test.tsx`) を追加し、ボタン click → mutation 呼出 検証。既存テスト (13 ファイル) に `vi.mock` 追加で tRPC provider 要求を回避。`pnpm --filter @claude-loom/ui test` 全 pass (1001 tests、75 test files)。TypeScript TS errors: ConsistencyView `Finding.id: string` / `Finding.file` の型整合を修正、pre-existing errors (usePlanMutations.ts / useProjectSettings.ts / routes.tsx / consistency-live.test.tsx) は t16 scope 外で放置。**rationale**: M0.15 Phase 5 t16 — Phase 2/3/4 で read-only にポートした 6 views の保存・操作ボタンを live daemon 変更に繋げる最終 hookup。

## M0.15 Phase 5 t17: PMChat write hookup

- **REQ-078**: `ui/src/live/usePMSession.ts` が新規作成され、`usePMSession()` hook を export する。hook は `start()` / `say(text: string)` / `permission(id: string, allow: boolean)` / `isLoading: boolean` を返し、3 mutation を tRPC (`trpc.pm.start` / `trpc.pm.say` / `trpc.pm.resolvePermission`) 経由で呼び出す。`ui/src/routing/AppShell.tsx` の PM chat handlers が fetch() stub から `usePMSession` 経由に置換される (`handlePmSend` → `pmSession.say(text)` / `handlePmStart` → `pmSession.start()` / `handlePmPermission` → `pmSession.permission(id, allow)`)。PMChatPanel / PMApprovalModal / PMApprovalToast は props (onSend / onStart / onPermission) 経由で mutation を呼び出す interface を維持する (SRP: write logic は AppShell / usePMSession に集約、view layer は props を呼ぶだけ)。`AppShell.test.tsx` + `AppShell.redesign.test.tsx` + `main.test.tsx` が `usePMSession` mock を追加して regression なし。`ui/test/views/pm-chat/pm-write.test.tsx` の 6 ケース (usePMSession module export / say / start / permission allow × 2 / permission reject) 全 pass。`pnpm --filter @claude-loom/ui test` 全 pass (992 tests)。**rationale**: M0.15 Phase 5 t17 — AppShell の fetch() stub PM handlers を tRPC mutation hook (usePMSession) に置換。実際の claude CLI spawn は将来 scope、daemon pm.* sub-router stub が受け口として既に存在し、frontend 側の wire hookup のみ本 task scope。

## M0.15 t4 Worktree (closure 補完)

- **REQ-071**: `ui/src/views/worktree/WorktreeView.tsx` + `SubroomView.tsx` が `useScenario().worktrees` (redesign/api/websocket) を data source とし、M0.11.4 ハードコード fixture を撤去。WorktreeView は branch graph + table (worktree-locked-badge / status / use 種別 / parent agent CatSprite / diskMB / lastCommit) を描画、SubroomView は `scenario.worktrees.find(branch)` で session derive。`ui/test/views/worktree/worktree-mock-active.test.tsx` の 5 ケース全 pass + 既存 worktree.test.tsx (9 tests、redesign 駆動再記述) + subroom-view.test.tsx (19 tests) regression なし。**rationale**: M0.15 Phase 2 batch A t4 — 既存 hardcode fixture を撤去し、redesign bundle の useScenario 経由データで Worktree view を read-only display に置換。本 entry は M0.15 closure 段階で補完 (t4 dev は REQ 採番時 t1 REQ-062 と衝突を予防するため REQ entry を append せず、closure t22 で PM 一括 append)。commit_sha: cebdbd8。

## M0.15 Phase 6 t18: redesign invariant harness test

- **REQ-079**: `tests/redesign_invariant_test.sh` (新規 bash harness test) が SPEC §3.6.14.3 「絶対消すな rule」の編集禁止 file (`redesign/scenarios.js` + `redesign/screens/*.jsx` × 12 + `redesign/Redesign App.html` + `redesign/cat.jsx` + `redesign/styles.css` + `redesign/tokens.css` + `redesign/_chat{1,2}.md` + `redesign/_BUNDLE_README.md` の計 20 file) を SHA-256 baseline (`tests/.redesign-invariant-baseline.txt`) と verify する。file 削除 or content 改竄を harness gate で構造的に検出。`tests/run_tests.sh` の auto-glob discovery で integrate、`bash tests/run_tests.sh` で 39 PASS / 1 pre-existing fail (loom_launch_ui、本 task 由来でなし)。RED phase で baseline 未保存時 fail / tamper test で 1 byte 改竄 fail を確認。**rationale**: M0.15 Phase 6 t18 — SPEC §3.6.14.3 で codify した mock fixture 保全規律を harness で gate 化、M0.15 期間中および以降の milestone でも mock fixture が誤って改変されることを構造的に防ぐ。commit_sha: 393c633。

## M0.15 Phase 6 t19: doc update SPEC + SCREEN_REQUIREMENTS + DOC_CONSISTENCY_CHECKLIST

- **REQ-080**: M0.15 closure 準備として 3 doc update を実施。(1) SPEC.md §3.6.14.6 の completion criteria checkbox を fulfilled 状態に近づけ (実装済み 5 項目を `[x]`、未着手 t18/t20/t21/t22 担当を `[ ]` のまま annotation)、(2) `docs/SCREEN_REQUIREMENTS.md` に「M0.15: useScenario shape SSoT (12 画面)」section を新設し各画面の destructuring + write paths + mock mode の 13 行 table を index 化、(3) `docs/DOC_CONSISTENCY_CHECKLIST.md` の M0.15 check items を t0-t18 実装結果に合わせて update (9/11 `[x]`、2 未済 = t20 Layer 2.5 / t22 closure tag chain verify)。SPEC §3.6.14.6 ↔ SCREEN_REQUIREMENTS ↔ DOC_CONSISTENCY_CHECKLIST の triangle が cross-reference 整合性 verify pass。**rationale**: M0.15 Phase 6 t19 — closure tag 設置前に doc consistency triangle を整える。M0.15 開始時に SPEC §3.6.14 を新設したが、実装完了後の completion criteria 反映 + 画面別 data dependency index 化 + checklist 更新が未着手だった。commit_sha: a4501e7。

## M0.15 Phase 6 t21: Playwright e2e baseline

- **REQ-081**: `ui/e2e/m0.15-redesign/screen-baseline.spec.ts` (新規) が 12 SPA route + PMChat overlay の `?mock=active` (一部 idle) screenshot baseline を取得し `ui/e2e/__screenshots__/m0.15-redesign/*.png` に 13 PNG として commit。`ui/e2e/m0.15-redesign/click-flow.spec.ts` (新規) が 重要 3 画面 (⑦ Customization 保存 / ⑬ PMChat 送信 / ⑫ Settings 保存) の 1-click flow interaction smoke を実行。既存 `ui/e2e/__screenshots__/room-baseline.spec.ts-snapshots/room-{pop,dusk,night}.png` も redesign 後の RoomView に合わせて regenerated。daemon 不在環境でも mock fallback で baseline 取得可能、click flow の REST assertion は daemon 起動時のみ assert (conditional)。`pnpm --filter @claude-loom/ui e2e` 全 19 pass (13 baseline + 3 click + 3 room)。`playwright.config.ts` の testDir: '.' が m0.15-redesign/ subdirectory を自動 cover、config 変更不要。**rationale**: M0.15 Phase 6 t21 — Layer 2 browser-interactive smoke を Playwright で baseline 化、Phase 2 以降の M0.15 関連 visual regression を構造的に check 可能に。commit_sha: af2c07f。

## M0.15 Phase 6 t20: Layer 2.5 dogfood smoke

- **REQ-082**: M0.15 closure 前の Layer 2.5 dogfood smoke を PM 直接実行で完了。SPEC §3.6.14.5 の 7 step matrix (launch hook → /health → /mode → / HEAD → 12 SPA route × 200 → browser visit 12 画面 → 重要 3 画面 1-click flow) を sequential 実行、全 step PASS。Step 6/7 は Playwright t21 (REQ-081) baseline (13 screenshot + 3 click flow = 19 pass) を evidence として採用。Step 1 で `~/.claude-loom/daemon.js` symlink 不在を発見 (auto-build chain gap)、`bash install.sh` re-run で resolve。結果は `docs/smoke-tests/m0.15-dogfood/report.md` に structured report として出力、検出 3 finding (auto-build chain gap / worktree isolation 機能不全 observation / REQ 番号衝突 pattern) を retro 候補として記録。**rationale**: M0.15 Phase 6 t20 — closure tag 設置前の最終 gate (SPEC §10.4.1 PM 必須 step)、F-USER-009 (retro 2026-05-06-003) で codify した Layer 2.5 規律遵守。本 smoke で発見した finding 3 件は M0.15 retro hook で process-axis lens 観察対象。

## M0.15 Phase 6 t22: closure tag + retro hook

- **REQ-083**: M0.15 closure marker として `m0.15-complete` tag を設置し、retro hook を trigger する。tag 設置時点で M0.15 全 22 task (t0-t22) が `status: done`、Layer 1 (1007 ui test + 39 bash harness + 19 Playwright e2e) + Layer 2 (Playwright baseline) + Layer 2.5 (PM dogfood 7 step) 全 PASS。tag annotation に M0.15 scope summary (12 画面 redesign port + reducer foundation + AppShell + posters + write API hookup + closure gates) + REQ-062..083 + tag chain (m0〜m5-complete + m0.15-complete) を含める。tag 設置直後に PM が user に「M0.15 完了、retro しとく？」を提案、user 承認時 `/loom-retro` 経由で `loom-retro-pm` dispatch。retro 中で本 closure report の検出 finding 3 件 (auto-build chain gap / worktree isolation 機能不全 / REQ 番号衝突 pattern) を process-axis lens の観察対象として継承。**rationale**: M0.15 Phase 6 t22 — milestone closure marker 設置 + retro hook trigger (CLAUDE.md M0.8 retro 規律遵守、agents/loom-pm.md "Milestone retro hook" section 整合)。Phase 1 closure trinity (M0.11.5/6/7) + Trust Recovery + UI Redesign Port (M0.15) で Phase 1 → Phase 2 boundary 完成、M1.0 Phase 2 Kickoff の事前条件達成。

## M0.16 Phase 1 t1+t2: snapshotPathTemplate OS-aware + baseline darwin migrate

- **REQ-084**: `ui/e2e/playwright.config.ts` の `snapshotPathTemplate` が `{snapshotDir}/{testFilePath}-snapshots/{arg}-{platform}{ext}` に refactor され、`{platform}` placeholder (Playwright internal token) で local darwin / CI linux 別 baseline 完全分離可能化。既存 16 baseline file (13 m0.15-redesign + 3 room-baseline) を `git mv` で `*-darwin.png` に rename、履歴保持。`pnpm --filter @claude-loom/ui exec playwright test --config e2e/playwright.config.ts e2e/m0.15-redesign/ e2e/room-baseline.spec.ts` で 16/16 baseline pass、ui vitest 1007 + daemon vitest 546 regression 0。**rationale**: M0.16 Phase 1 (a58ad64) — retro 2026-05-12-001 F-res-002 で codify した OS-aware baseline structural fix の foundation。Phase 2 (CI Linux baseline 自動生成) の前提条件確立。

## M0.16 Phase 2 t3: playwright-regenerate.yml workflow

- **REQ-085**: `.github/workflows/playwright-regenerate.yml` を新規 file として新設 (ci.yml と SRP 分離)、`workflow_dispatch` trigger + 2 input (target/pr-title) + `--update-snapshots` step + auto-PR flow を実装。`permissions:` block は `contents: write` + `pull-requests: write` のみ (最小権限)、`secrets.GITHUB_TOKEN` only (PAT 非使用)、branch naming injection 防止は `$(date +%Y%m%d-%H%M%S)` 確定 suffix で対応。`tests/m0116_playwright_regenerate_workflow_test.sh` (17 check 全 pass) で yaml syntax + permission scope + GITHUB_TOKEN-only + injection prevention を harness verify。**rationale**: M0.16 Phase 2 (d1e1530) — CI Linux baseline 自動生成 + auto-PR で push 前 CI red を構造解消、local macOS / CI linux 両環境の baseline parity を確立。trio 3-aspect path C self-review (code + security + test + SPEC §3.6.15 cross-check) all pass。

## M0.16 Phase 3 t6: agents/loom-pm.md Layer 2.5 Step 8 act codify

- **REQ-086**: `agents/loom-pm.md` の Layer 2.5 dogfood smoke 実行手順 (line ~412-426) に **Step 8 = `act -W .github/workflows/ci.yml pull_request --container-architecture linux/amd64`** を必須 step として追加。graceful fallback 規律 (Docker daemon 不在時 / act binary 不在時 / macOS Apple Silicon の `linux/amd64` flag) を SPEC §3.6.15.4 と整合する形で記述。Step 1-7 BLOCK / Step 8 graceful exception の asymmetric failure handling 明示。**rationale**: M0.16 Phase 3 (ab901ea) — agent prompt SSoT と SPEC §3.6.14.5 / §3.6.15.4 SSoT の double-write で実装、PM が closure 時に Step 8 を invoke する執行責務を agent prompt 側にも codify。

## M0.16 Phase 3 t7: tests/act_smoke_test.sh graceful skip harness

- **REQ-087**: `tests/act_smoke_test.sh` を新規 file として新設、`act` binary check + Docker daemon check (両方 `command -v` + `docker info &>/dev/null`) で graceful skip pattern を実装、不在時 `exit 0` で closure 自体は block しない。両方揃った時のみ `act -W .github/workflows/ci.yml pull_request --container-architecture linux/amd64` を実 invoke。`tests/run_tests.sh` の auto-glob (`*_test.sh` pattern) で自動 discover、`chmod +x` 実行 permission 設定済。**rationale**: M0.16 Phase 3 (db3c5d6) — SPEC §3.6.15.4 graceful fallback 規律の harness layer。`tests/redesign_invariant_test.sh` (M0.15 t18) と同層の bash test として整理、Docker 不在 dev 環境でも closure block しない user-friendly design。

## M0.16 Phase 4 t8: DOC_CONSISTENCY + SCREEN_REQUIREMENTS M0.16 update

- **REQ-088**: `docs/DOC_CONSISTENCY_CHECKLIST.md` の M0.16 section 9 check items のうち実装完了 6 項目を `[ ]` → `[x]` に update (snapshotPathTemplate / baseline OS suffix / CI workflow_dispatch / SPEC §3.6.14.5 Step 8 codify / agents/loom-pm.md / act_smoke_test.sh)、各 `[x]` に commit SHA 付記。残 3 項目 (learned_guidance ttl expire / retro F-res-002 status update / closure tag chain) は `[ ]` 残置で t11 PM closure 予定を注記。CI workflow item 文言を実装実態 (`playwright-regenerate.yml`、ci.yml 統合じゃなく独立 file 新設) に合わせて正確化。`docs/SCREEN_REQUIREMENTS.md` 変更履歴 section に M0.16 minimal mention 1 行追加 (「Playwright e2e baseline OS-aware 化、画面要件には影響なし」)。**rationale**: M0.16 Phase 4 (152b067) — closure tag 設置前の doc consistency 整合性確保、9 check items の SSoT cross-reference (SPEC §3.6.15 ↔ commit history ↔ check items) を audit 可能化。

## M0.16 Phase 4 t10: Layer 2.5 dogfood smoke

- **REQ-089**: M0.16 closure 前の Layer 2.5 dogfood smoke を PM 直接実行で完了 (SPEC §10.4.1 + §3.6.14.5 + §3.6.15.4)。**8 step matrix** (Step 1-7 既存 + Step 8 act invocation 新規) を sequential 実行: Step 1-5 (launch hook / /health / /mode / / HEAD / 12 SPA route) 全 PASS、Step 6/7 (Playwright baseline + click flow) は M0.16 t1+t2 (a58ad64) で darwin baseline migrate 済の evidence で代替 PASS、Step 8 (act invocation) は **graceful skip** (act binary 不在、SPEC §3.6.15.4 期待動作の dogfood validation)。`docs/smoke-tests/m0.16-dogfood/report.md` に structured report 出力、検出 finding 2 件 (act binary adoption gap / post-closure CI verify dependency) を retro 候補として記録。**rationale**: M0.16 Phase 4 (t10、PM direct) — closure tag 設置前の最終 gate (SPEC §10.4.1 PM 必須 step)。Step 8 graceful skip path が SPEC §3.6.15.4 通り function した evidence は本 milestone の structural value の一部、user dev 環境で act binary 不在でも closure block しない設計の dogfood 検証成立。

## M0.16 Phase 4 t11: closure tag + retro hook + lg ttl expire

## M0.17 Phase 4.5: AppShell marginRight wrapper + stale Playwright test selector hotfix

- **REQ-091**: M0.17 Phase 4.5 hotfix — proposed AppShell で drop された RoomView marginRight wrapper を復活し、LiveRail/PMChatPanel overlay が PM desk (x=W*0.78) を覆う構造的 bug を修正。screen-baseline.spec.ts の gantt/consistency/retro waitSelector を旧 dialog testid `view-panel` から view 固有 testid (`gantt-view` / `consistency-view` / `retro-view`) に更新。click-flow 3 件 (Customization / PMChat / ProjectSettings) の network assertion を tRPC wsLink WS transport 実態に合わせ修正 (HTTP route intercept では WS mutations 捕捉不能)。GanttView + RetroView の root div に `data-testid="gantt-view"` / `data-testid="retro-view"` 追加。Playwright darwin baseline 16 枚再撮影、19/19 pass 確認。**rationale**: M0.17 UI Redesign Port Phase 4 commit 後の Playwright baseline retake で 7 failures 検出、root cause A (AppShell wrapper drop) + root cause B (stale selector) + root cause C (HTTP intercept vs WS transport mismatch)。

## M0.17 Phase 1 t1+t2: shell.css 復旧 + index.css 配線 + tokens.css verify

- **REQ-092**: `ui/src/styles/shell.css` (832L) が `redesign/Redesign App.html <style>` block (L8-280) の verbatim port、shell layout classes (`.shell` / `.top` / `.top__*` / `.drawer` / `.nav-link` / `.content` / `.statusbar` / `.scenario-picker` / `.pm-chat` / `.pm-toast` / `.pm-modal` / `.rail` / `.coldstart` / `.cat-walker` / `.btn-px` 等) を所有。`ui/src/styles/index.css` に `@import './shell.css';` を `@import './tokens.css';` 直後に配線 (`@import './room.css';` は t9 atomic)。`ui/src/styles/tokens.css` の Phase 1 必須 25 token verify、`--p-ok` + `--p-bad` の 2 token を `:root` / `.theme-dusk` / `.theme-night` 3 theme block に追加 (23 token は pre-existing)。`pnpm --filter @claude-loom/ui test` 1007/1007 + `pnpm --filter @claude-loom/daemon test` 546/546 + `tsc --noEmit` 0 new error。**rationale**: M0.17 Phase 1 (fd81c22) — REVIEW.md §B1 致命修正 (shell CSS 移植漏れで TopBar 縦並び / Drawer viewport 外 / StatusBar 不在 / ScenarioPicker layout 崩壊)、shell 復旧で再現度 30〜40% → ~80% 回復見込み (REVIEW.md 「これを直すと画面の8割は治る」)。

## M0.17 Phase 2 t4+t5+t6: ゾーン SVG ラグ化 + 比率レイアウト + Islands 削除

- **REQ-093**: `ui/src/styles/tokens.css` 末尾に tokens.css.patch (69L) append (zone tints `--p-zone-{dev,pm,review}` + `-fg`、decorative props `--p-prop-{plant-leaf,plant-leaf-2,plant-pot,shelf,shelf-band,cooler-body,cooler-water}`、z-index scale `--z-{room-base,room-decor,room-agents,room-walkers,rail,pm-chat,pm-handle,pm-toast,agent-detail,pm-modal,toast}`、room layout ratios `--room-{floor,pm-x,dev-x,rev-code-x,rev-test-x,rev-sec-x,zone-dev-w,zone-pm-h}-ratio`、bubble vars + desk dimensions)、9 個の `.room-island*` rule 削除。`ui/src/views/room/RoomBackground.tsx` を proposed 内容 (173L) で置換、SVG `<rect opacity="0.3">` + `<text opacity="0.16" letterSpacing="8">` で zone を描画 (枠線なし、redesign/screens/room.jsx L143-244 SSoT)、decorative props (plant + shelf + cooler) も SVG 化。`ui/src/views/room/constants.ts` 新設 (86L、`ROOM_AGENT_IDS` / `ZONES` / `COLD_START_COPY` / `LIVE_RAIL_TABS` 等 export)。`ui/src/views/room/RoomView.tsx` を proposed 内容 (285L) で置換、`width`/`height` props 廃止 + ResizeObserver + `floorY = H * 0.43` 比率座標 + 4 モーダル撤去 + ポスター `onClick={navigate('/...')}` 配線 + SUBROOM_CLONES `scenario.worktrees.filter().slice(0, 4).map()` + `<Plant>` DOM 削除。`ui/src/views/room/Islands.tsx` (56L) 削除。7 test files refactored、`islands-decor.test.tsx` (316L) 削除。`pnpm test` UI 956/956 + daemon 546/546。**rationale**: M0.17 Phase 2 (d737f89) — REVIEW.md §B2 (ゾーン箱化 → SVG ラグ化) + §B3 (固定座標 → ResizeObserver 比率) + §S3 (REVIEW デスク T 字 → 横並び) + §S4 (worktree clones デスク右 → デスク上) + §S5 (Plant DOM 削除、SVG プロップに統合) の atomic fix。

## M0.17 Phase 3 t8+t9+t10: Outlet sibling routing + LiveRail + room.css G6

- **REQ-094**: `ui/src/routing/AppShell.tsx` を proposed 内容 (295L) で置換 (468L→295L)、`isRoom ? <RoomView /> : <Outlet />` の sibling routing 化 (旧 dialog overlay 撤去)、Escape key handler 削除、`NAV_GROUPS` を `./constants` から import、`APP_COPY` 経由で文字列 token 化、LiveRail mount 配線 (`showPmPanel ? <PMChatPanel> : <LiveRail>`)。`ui/src/routing/constants.ts` 新設 (74L、`NAV_GROUPS` 3 groups × 11 items / `SCENARIO_KEYS` / `APP_COPY` export)。`ui/src/styles/room.css` 新設 (366L、`.desk-station__*` 等 class)。`ui/src/styles/index.css` に `@import './room.css';` 追加 (final 3 import: tokens → shell → room → tailwind)。`ui/src/views/room/LiveRail.tsx` 新設 (92L、PM idle fallback、tabs: merged/reasoning/tools)、`StreamEvent` 型名 stale を dev が `StreamMsg` (redesign/api/types.ts SSoT) に独自修正。`ui/src/views/room/DeskStation.tsx` を proposed 内容 (141L) で置換 (253L→141L)、inline style 全廃 → `.desk-station__*` クラス、`STATUS_COLOR` 定数廃止 → modifier class、`deskColor` prop 廃止、動的座標 `{left, top}` のみ inline 例外、`data-testid` 完全保持。10 個別 screen verify (全 10 既に clean、修正不要)。`pnpm test` UI 953/953 + daemon 546/546。**rationale**: M0.17 Phase 3 (dc00834) — REVIEW.md §S1 (LiveRail PM idle fallback) + §S2 (sibling routing で Drawer active 強調活性化) + §G1 (CSS 一極化 → inline 全廃) + §G6 (構造化テーブル + 文字列 token 化) の atomic fix。proposed file の `StreamEvent` typo dev 検出 + 修正は self-review path C の品質効果実例。

## M0.17 Phase 4 t12: cat-walker walkTo wire (S6)

- **REQ-095**: `ui/src/views/room/DeskStation.tsx` に optional prop `walkTo?: { dx: number; dy: number }` 追加、`walkTo` 定義時に root div に `.cat-walker` class 付与 + `style` で `--walk-dx` / `--walk-dy` CSS 変数 inject、未定義時 dormant。`ui/src/views/room/RoomView.tsx` で `scenario.agents[id].walkTo` (実態は agent id string、redesign/api/types.ts SSoT 整合) を receive、positions table lookup 経由で `{ dx: target.x - my.x, dy: target.y - my.y }` 形式に変換して DeskStation に prop pass (redesign source room.jsx L460-462 と integer-precision 一致)。`ui/test/views/room/desk-station.test.tsx` に walkTo TDD tests +5 (RED 3 failures before implementation、GREEN pass 後)。shell.css L463-484 の `.cat-walker` + `@keyframes cat-walk-trip` + `@keyframes cat-bubble` は Phase 1 で port 済、本 task は配線 only。`pnpm test` UI 958/958 + daemon 546/546。**rationale**: M0.17 Phase 4 (cc8c9d5) — REVIEW.md §S6 (cat-walker 配線未実装、優先度低い前提で Phase 4 polish 扱い)、redesign source の `state.walkTo` 機構 (隣ゾーン歩行 8 秒 ease-in-out infinite アニメーション) を DeskStation prop 経由で active 化。

## M0.17 Phase 4 t13+t14: RoomModeToggle 撤去 + M2 branch label + M3 font + SubroomClone/bubble verify

- **REQ-096**: `ui/src/views/room/decor/RoomModeToggle.tsx` (Room 画面右上 「🐱 レトロ開始」CTA) delete (`git rm`)、Phase 2 RoomView replacement で既に呼び出し dormant 化済、import 無し safe deletion、`decor/index.ts` の barrel export からも削除。`ui/src/views/room/decor/RoomWallDecor.tsx` の M2 branch label を hardcoded `◆ claude-loom — branch: main` から `◆ branch: {branch}` (prop-driven、default `'main'`) に修正、`scenario.branch` を RoomView 経由で prop pass、`claude-loom` 名は TopBar 責務と整合 (REVIEW.md §M2)。M3 font 確認: shell.css L30 で body `font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace` 直接適用 (Phase 1 で port 済)、`--font-sans` を override、tokens.css `--font-sans` は Tailwind preflight 用 fallback として残置 (no-op verify)。SubroomClone label 重なり verify (room.css L217 で既に非重複設計、no-op)、Speech bubble tail verify (room.css L49-60 で `rotate(45deg)` 実装済、no-op)。**rationale**: M0.17 Phase 4 (cc8c9d5) — REVIEW.md §M1 (RoomModeToggle 撤去判断 → 設計案 = Drawer MANAGE グループ経由のみに統一) + §M2 (branch label 責務分離) + §M3 (font 整合性確認) + §S6 後段 (SubroomClone + Speech bubble verify) の Phase 4 polish 統合 cleanup。

## M0.17 Phase 5 t16: doc update (PLAN.md + SCREEN_REQUIREMENTS + DOC_CONSISTENCY)

- **REQ-097**: `PLAN.md` の M0.17 t1-t15 + Phase 4.5 hotfix 全 entry を `[x]` + `status: done committed_sha: <SHA>` + path / planned_files 詳細で update (5 feat + 1 fix commit chain: fd81c22 → d737f89 → dc00834 → cc8c9d5 → 7215138 + spec phase 10c7154)、t3/t7/t11 を `status: deferred-to-t15` に reclassify、Phase 4.5 hotfix section を Phase 4 完成基準と Phase 5 closure の間に新設挿入 (3 root cause + dev hotfix dispatch + Playwright 19/19 pass 記録)。`docs/SCREEN_REQUIREMENTS.md` 変更履歴 section に 2026-05-14 entry 追加 (M0.17 UI Redesign Port Correction の Phase 1-4 + Phase 4.5 hotfix scope summary + 再現度 ≥90% 達成)。`docs/DOC_CONSISTENCY_CHECKLIST.md` に M0.17 section 新設 (19 check items: 16 [x] 完了 + 3 [ ] closure 予定)。**rationale**: M0.17 Phase 5 (t16) — 5 feat + 1 fix commit chain の SSoT cross-reference (REVIEW.md ↔ commit history ↔ PLAN.md task entries ↔ SCREEN_REQUIREMENTS changelog ↔ DOC_CONSISTENCY check items) を audit 可能化、SPEC §3.6.14.3 規律 (PM 一括 doc update) 遵守。

## M0.17 Phase 5 t18: Layer 2.5 dogfood smoke 8 step

- **REQ-098**: M0.17 closure 前の Layer 2.5 dogfood smoke を PM 直接実行で完了 (SPEC §10.4.1 + §3.6.14.5 + §3.6.15.4)。**8 step matrix** (Step 1-7 既存 + Step 8 act invocation) を sequential 実行: Step 1 (`bash hooks/loom-launch-ui.sh`) / Step 2 (`curl /health` → `{"status":"ok"}`) / Step 3 (`curl /mode` → 6 field) / Step 4 (`curl / HEAD` → `200 text/html`) / Step 5 (12 SPA route smoke、sibling routing で Drawer click → URL → content 切替確認) / Step 6/7 (M0.17 t15 + Phase 4.5 7215138 で 16 darwin baseline 再撮影 + click flow 3/3 pass 済の evidence で代替 PASS) / Step 8 (`act` invocation、graceful skip 可)。`docs/smoke-tests/m0.17-dogfood/report.md` に structured report 出力、検出 finding (sibling routing visual regression 候補 / cat-walker walkTo 実機動作 / RoomModeToggle 撤去 confirm) を retro 候補として記録。**rationale**: M0.17 Phase 5 (t18、PM direct) — closure tag 設置前の最終 gate (SPEC §10.4.1 PM 必須 step)、M0.15/M0.16 Layer 2.5 dogfood smoke pattern 継承。

## M0.17 Phase 5 t19: closure tag + retro hook + main PR open

- **REQ-099**: M0.17 closure marker として `m0.17-complete` tag を設置し、retro hook trigger + main への PR open (branch hygiene learned_guidance lg-2026-05-12-001 遵守) を完了。tag 設置時点で M0.17 全 19 task (t1-t19) + Phase 4.5 hotfix が `status: done`。Layer 1 (UI Vitest 958 + daemon Vitest 546 + bash run_tests.sh 全 PASS) + Layer 2 (Playwright darwin baseline 19/19 pass) + Layer 2.5 (PM 直接 dogfood smoke 8 step 全 PASS) 全達成。tag annotation に M0.17 scope summary + REQ-091..099 + tag chain (`m0` 〜 `m0.16-complete` + `m0.17-complete`) を含める。tag 設置直後に `gh pr create` で main への PR open、`yutron24ah/claude-loom` main 取込 trigger。retro hook で user に「M0.17 完了、retro しとく？」提案、user 判断に従う。**rationale**: M0.17 Phase 5 (t19、PM direct) — milestone closure marker 設置 + branch hygiene PR open trigger (lg-2026-05-12-001 codify した「milestone tag 設置直後 + 32+ commits ahead of main を観測したら、main への PR open を user に提案」の遵守実例) + retro hook (M0.16 Phase 2 hardening continuation の第 3 milestone 完成、dogfood-trinity continuation marker chain 継続: Phase 1 trinity → UI Redesign Port (M0.15) → Playwright OS-aware Baseline (M0.16) → UI Redesign Port Correction (M0.17))。

## M0.17 post-tag-hotfix series (a50f0a9 / 84af3f3 / 6ed212b)

m0.17-complete tag 設置後 (commit 170d323)、PR #12 push で CI が Linux Playwright baseline 不在で 16 failures、原因究明で M0.16 由来の latent bug 2 件 + repo setting 制約 1 件を順次解消した hotfix series。SPEC §3.6.8.11 post-tag-hotfix protocol 遵守 (tag 移動禁止、`[post-tag-hotfix m0.17-complete]` annotation 全 commit、同 branch 継続、retro scope 必須 inclusion 確約)。

- **REQ-100**: `.github/workflows/playwright-regenerate.yml` を全面 simplify (147L → 92L、commit `a50f0a9`)。M0.16 codify 時の GH Actions YAML parse 不全 (leading 24 行 comment block 過剰 + `pr-title` dash-named input + 複雑な escape の組合せで GH parser fallback、workflow name が path で registration、workflow_dispatch trigger 認識不全 → `gh workflow run` で HTTP 422 "Workflow does not have 'workflow_dispatch' trigger" 連続) を構造修正。leading comments 簡素化 + `pr-title` 廃止 (auto title generation で代替) + `--update-snapshots=all` 構文整合 + base branch を `github.ref_name` で dynamic 化 (feature branch invoke 時 auto-PR が同 branch に向く)。**rationale**: M0.16 で codify した CI parity gate が 0/2 milestone で機能しとらん事象を構造修正、Linux baseline auto-generation infrastructure を実 active 化。

- **REQ-101**: `.github/workflows/playwright-regenerate.yml` の detect-changes step を `git diff --quiet` から `git status --porcelain` に変更 (commit `84af3f3`)。M0.16 codify 時の logic bug — `git diff` は untracked file を検出しないため、初回 Linux baseline 16 枚生成 (全部 untracked 新規 file) を「変更なし」と誤判定し Create PR step が silent skip されとった。git status --porcelain は untracked + modified 両方検出。**rationale**: REQ-100 で workflow parse は通ったが Create PR が skip され auto-PR 出ない症状を再 invoke run 25864168844 で発見、git tracking semantics に整合する fix。M0.17 PR #12 で初めて Linux baseline 自動 PR を機能させた precedent commit。

- **REQ-102**: Linux baseline `chore/playwright-linux-baseline-20260514-135845` branch (workflow が生成 + push 済、`gh pr create` のみ "GitHub Actions is not permitted to create or approve pull requests" repo setting 制約で fail) を fix/m0.17-ui-redesign-correction branch に `--no-ff` merge (commit `6ed212b`)。16 Linux baseline file (`agent-detail-linux.png` / `consistency-linux.png` / `customization-linux.png` / `gantt-linux.png` / `guidance-linux.png` / `plan-linux.png` / `pm-chat-overlay-linux.png` / `project-settings-linux.png` / `retro-linux.png` / `room-linux.png` / `sessions-linux.png` / `tokens-linux.png` / `worktree-linux.png` + 3 room theme baseline) を M0.17 PR に取り込み、CI Linux Playwright が darwin baseline (M0.17 Phase 4.5 7215138) + linux baseline (本 commit) 揃った状態で 19/19 pass 達成。**rationale**: repo setting "Allow GitHub Actions to create and approve PRs" 無効が原因の Create PR step fail を direct branch merge で workaround、admin access 不要で M0.17 closure path を確立。Phase 2 hardening retro candidate: repo setting 整合性 audit (M0.16 codify の前提と repo 実態の乖離検出 mechanism)。

## M0.17 Phase 4.8: Production mock= scaffolding gate

- **REQ-103**: post-closure user 指摘「production code に mock= 残っとる」を契機に、`redesign/api/websocket.ts` の `useScenario()` mock fallback block を `if (import.meta.env.DEV)` で gate + `ui/src/routing/AppShell.tsx` の `<ScenarioPicker />` render を `{isRoom && import.meta.env.DEV && <ScenarioPicker .../>}` で gate + `ui/tsconfig.json` に `"types": ["vite/client"]` 追加 (`ImportMeta.env` 型解決の collateral、副次的に `useProjectSettings.ts` の pre-existing tsc error 1 件解消)。production build (Vite minifier + daemon-served ui/dist) では `SCENARIOS` / `ScenarioPicker` function / `readMockKey()` が Vite DCE (static `import.meta.env.DEV` 置換 + tree-shake) で完全除去、`http://127.0.0.1:5757/?mock=active` 経由の real daemon bypass が構造的に不能化。dev mode (Vite dev server) + Vitest test (DEV=true) では従来動作維持で Vitest 958/958 + daemon 546/546 + bash 42/42 + Playwright 19/19 regression 0、tsc -1 net (32→32、新 error 0 + pre-existing 1 件副次解消で 33→32)。`redesign/scenarios.js` fixture data は M0.15 frozen scope のため source 残置、production bundle からは Vite tree-shake で除去 (caller 不在化)。`useScenarioMockKey()` export は Vitest 5 test files の `vi.mock` override が依存するため untouched 残置。**rationale**: SCREEN_REQUIREMENTS 「mock mode: ?mock=active で fixture 注入」が M0.15 redesign 期間中の design 前提のまま production release scope へ leak、user が real daemon verify で発覚。build-time gate (Vite `import.meta.env.DEV` 静的置換) は production user が runtime で書き換え不能の構造的 security 改善、URL-based mock bypass を tree-shake で物理的除去。M0.17 post-tag-hotfix series 第 4 件 (a50f0a9 / 84af3f3 / 6ed212b / 559549e)、SPEC §3.6.8.11 規律遵守 (tag 不変、同 branch 継続、commit message `[post-tag-hotfix m0.17-complete]` annotation、retro scope 必須 inclusion)。

## M0.17 Round 2 Phase A: 動かないボタン 8 件解消 (B4-B11)

- **REQ-104**: ColdStart の「▶ PM を起動」ボタンが `usePMSession().start()` を呼ぶ。`alert()` placeholder は削除。`ui/src/views/room/RoomView.tsx` で `usePMSession` hook を import + call。test: `test/views/room/coldstart-b4.test.tsx`。

- **REQ-105**: `ScenarioPicker.activate()` が `window.history.replaceState` + manual `popstate` dispatch ではなく React Router の `useSearchParams` + `setSearchParams` を使う。MemoryRouter 上で URL state が React として同期される (re-render trigger)。`window.history.replaceState` は呼ばれない。test: `test/routing/scenario-picker-b11.test.tsx`。

- **REQ-106**: M0.17 Round 2 Phase C 細部 cleanup。(S8) TopBar に branch chip (`data-testid="topbar-branch"`) を追加、`scenario.branch` を `◆ branch: {branch}` で常時表示、project chip (B7 navigate) は併存維持。`shell.css` に `.top__branch` style 追加。(M5) `SCENARIO_KEYS` に `'live'` を追加 (4 elements)、ScenarioPicker の別実装 'live' button を削除し unified loop (`k === 'live' ? '' : k` でactivate) に統一。(S9) `ui/src` TSX 内の `zIndex: <数値>` 生数値 5 件を `var(--z-*)` token に置換 (`--z-agent-detail` / `--z-room-walkers` / `--z-pm-chat` / `--z-room-agents`)。(M4) `routes.tsx` comment 「Panel overlay routes」→「Sibling screen routes」更新。(M3) `shell.css` cat-walker verify — `@keyframes cat-walk-trip` + `.cat-walker` + `var(--walk-dx)/var(--walk-dy)` 参照 healthy 確認 (no-op)。(S7) wrapper 維持判断: `room.jsx` の `W = chatOpen ? size.w - CHAT_W : size.w` + `pm.x = W*0.78` が設計 SSoT、wrapper が正確に `W` 縮小を再現しとることを確認 (design-verified)。test: `test/routing/phase-c-cleanup.test.tsx`。

## M0.17 Round 3 Phase D: Room polish to 95% (6 redesign source gaps)

- **REQ-107**: `tokens.css` に `--desk-container-width: 110px` を追加し `--desk-width: 96px` と分離。`room.css` の `.desk-station` container を `--desk-container-width` へ切替え (redesign L29: container width 110)、`.desk-station__top` / `.desk-station__front` は引き続き `--desk-width: 96px` を使用 (redesign L74-75: inner desk visual 96px)。test: `test/views/room/phase-d-polish.test.tsx`。

- **REQ-108**: DeskStation の monitor lines を 4 本 → 3 本に削減。redesign source L65-68 整合 (70%/50%/85% の 3 lines)。`DeskStation.tsx` の `--w40` line div を削除、`room.css` の `.desk-station__monitor-line--w40` dead class を削除。test: `test/views/room/phase-d-polish.test.tsx`。

- **REQ-109**: DeskStation に typed bubble prop `{ kind: 'tool' | 'reason'; text: string; sub?: string }` を追加。`kind='tool'` 時は `.desk-station__bubble-tool` span (黄色背景 --p-warn) + optional `.desk-station__bubble-sub` (28 chars truncated)。`kind='reason'` 時は `.desk-station__bubble-reason` span (italic quote)。`task` prop は backward compat のため残置。`RoomView.toBubble()` を typed `BubbleShape` 返却に更新、`bubble` prop 経由で渡す。test: `test/views/room/phase-d-polish.test.tsx`。

- **REQ-110**: PMChatPanel STREAM tab のストリーム行を `rail__line ${s.kind}` クラス付き div + `.ts` / `.who` / `.tool` / `.text` span で render (redesign L344-356 整合)。tabs row に close button (▶) を追加 (`data-testid="pm-chat-close"`)、`onToggle` prop 経由で AppShell の collapse 制御に接続。test: `test/views/room/phase-d-polish.test.tsx`。

- **REQ-111**: PMChatPanel に `collapsed?: boolean` + `onToggle?: () => void` prop を追加。`collapsed=true` 時は vertical `.pm-chat-handle` button を return (redesign L303-308)、stream count badge 表示 + onToggle wiring。AppShell に `pmChatCollapsed` state を追加し PMChatPanel に prop pass。test: `test/views/room/phase-d-polish.test.tsx`。

- **REQ-112**: RoomWallDecor の wall sign positioning を redesign L443-444 に整合。branch sign: `left: 14, top: 10` (旧 left:20, top:14)。clock sign: `right: 14, top: 10` (旧 left:width-150、CSS right property に切替)。`now` prop 追加で `scenario.now` を表示 (旧ハードコード "14:23 JST")。`width` prop は deprecated optional 化。test: visual + Playwright baseline retake で検証。

- **REQ-090**: M0.16 closure marker として `m0.16-complete` tag を設置し、retro hook trigger + `lg-2026-05-13-001` ttl expire を完了。tag 設置時点で M0.16 全 11 task (t1-t11) が `status: done` (t4 のみ post-closure-verify scope で本 PR merge 後 user invoke、それ以外 done)。Layer 1 (ui 1007 + daemon 546 + bash run_tests.sh 41+1pass) + Layer 2 (Playwright t21 baseline + click flow、M0.16 t1+t2 で darwin migrate 済) + Layer 2.5 (8 step、Step 8 graceful skip dogfood validated) 全 PASS。tag annotation に M0.16 scope summary + REQ-084..090 + tag chain (m0〜m0.15-complete + m0.16-complete) 含める。`.claude-loom/project-prefs.json` の `agents.loom-pm.learned_guidance[].id == "lg-2026-05-13-001"` を `active: false / ttl: expired / expired_at: "2026-05-13" / expired_by: "m0.16-complete (SPEC §3.6.15 + §3.6.14.5 Step 8 formal codify)"` に切替え、暫定規律から formal SPEC 規律へ移行。`pending.json` の retro 2026-05-12-001 F-res-002 entry に M0.16 完了 reference 追記。retro hook で user に「M0.16 完了、retro しとく？」提案、user 判断に従う。**rationale**: M0.16 Phase 4 (t11、PM direct) — milestone closure marker 設置 + learned_guidance loop の structural validation (lg-2026-05-13-001 が暫定→SPEC formal 規律で代替され ttl expire するという M0.11 codify した learned_guidance ttl/decay 機構の dogfood)。Phase 2 hardening continuation の第 2 milestone 完成、M0.15 → M0.16 で dogfood-trinity continuation marker chain が継続 (Phase 1 trinity → UI Redesign Port → Playwright OS-aware Baseline)。
